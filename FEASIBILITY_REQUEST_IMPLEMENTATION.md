# خطة تنفيذ نظام طلب دراسة الجدوى الجديد

## 📋 نظرة عامة

تنفيذ نظام جديد لطلب دراسات الجدوى مع صفحة مستقلة وزر في الـ Navbar، مع التحقق من رقم الهاتف عبر OTP.

---

## 🎯 المتطلبات

### 1. صفحة جديدة لطلب دراسة الجدوى (`/feasibility-request`)
- فورم شامل بدون تسجيل دخول
- التحقق من رقم الهاتف عبر OTP
- **نوعين من دراسات الجدوى** (انظر القسم التالي)

### 2. نوعين من دراسات الجدوى

#### 🏠 النوع الأول: دراسة جدوى بدون نزول ميداني
**المميزات:**
- تحليل للمنطقة والسوق
- دراسة المنافسين والأسعار
- توقعات الإيرادات
- **لا تتضمن**: تكوين الغرف وتكلفة الأثاث والتجهيزات (ميكرويف، ثلاجة، طاولات...)
- **سعر خاص** (أقل من الدراسة الميدانية)

**محتويات الدراسة عند الإنشاء:**
- المقدمة (Cover + Introduction)
- تكوين الشقة (Room Setup) - عدد الغرف فقط بدون تفاصيل الأثاث
- دراسة المنطقة (Area Study + Map + Nearby Apartments)
- الإحصائيات (Statistics)
- الخاتمة (Footer)

#### 🔍 النوع الثاني: دراسة جدوى مع نزول ميداني
**المميزات:**
- كل ما في النوع الأول
- **بالإضافة إلى**: زيارة ميدانية للشقة
- تكوين كامل للغرف مع الأثاث والتجهيزات
- تكلفة تفصيلية لكل غرفة
- ملخص التكاليف الإجمالية
- **سعر خاص** (أعلى)

**محتويات الدراسة عند الإنشاء:**
- المقدمة (Cover + Introduction)
- تكوين الشقة (Room Setup)
- شرائح الغرف التفصيلية (Bedroom, Kitchen, Bathroom, Living Room)
- ملخص التكاليف (Cost Summary)
- دراسة المنطقة (Area Study + Map + Nearby Apartments)
- الإحصائيات (Statistics)
- الخاتمة (Footer)

### 3. تعديل قسم CTA
- إبقاء فورم التواصل فقط (بدون تكوين الشقة)
- إزالة خيار "أضف تفاصيل شقتك"

### 4. تعديل قسم الخدمات (ServicesSection)
- **إزالة الخدمات المؤقتة الحالية**
- **إضافة بطاقتين فقط:**
  1. دراسة جدوى بدون نزول ميداني
  2. دراسة جدوى مع نزول ميداني
- كل بطاقة ترتبط مباشرة بالفورم مع تحديد النوع تلقائياً

### 5. طريقة الوصول للصفحة

#### أ) من الـ Navbar:
```
┌─────────────────────────────────────────────┐
│   اختر نوع دراسة الجدوى                    │
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────┐  ┌─────────────────┐  │
│  │                 │  │                 │  │
│  │   📊 بدون      │  │   🔍 مع        │  │
│  │   نزول ميداني  │  │   نزول ميداني  │  │
│  │                 │  │                 │  │
│  │  - تحليل السوق │  │  - كل ما سبق   │  │
│  │  - دراسة المنطقة│  │  - زيارة ميدانية│  │
│  │  - الإيرادات   │  │  - تجهيز الشقة  │  │
│  │                 │  │  - التكاليف    │  │
│  │   [اختر]       │  │   [اختر]       │  │
│  └─────────────────┘  └─────────────────┘  │
│                                             │
└─────────────────────────────────────────────┘
```

#### ب) من قسم الخدمات:
- يفتح الفورم مباشرة بدون عرض الاختيارات
- النوع محدد مسبقاً حسب البطاقة المضغوط عليها

### 6. بيانات فورم طلب دراسة الجدوى
| الحقل | النوع | مطلوب |
|-------|------|-------|
| نوع الدراسة | اختيار (مع/بدون نزول ميداني) | ✅ |
| اسم العميل (الأول + الأخير) | نص | ✅ |
| نوع العقار | اختيار | ✅ |
| عدد الغرف (غرف نوم، صالات، مطابخ، حمامات) | أرقام | ✅ |
| اسم المنطقة | نص | ✅ |
| اسم الحي | نص | ✅ |
| اسم الشارع | نص | ✅ |
| رقم العمارة | نص | ✅ |
| رابط جوجل ماب (اختياري) | رابط | ❌ |
| رقم الهاتف + التحقق OTP | هاتف + رمز | ✅ |

---

## 📊 المراحل

---

## 🚀 المرحلة 1: تحديث قاعدة البيانات (الأولوية: عالية جداً)

### 1.1 إنشاء موديل جديد `FeasibilityRequest`

**الملف:** `prisma/schema.prisma`

```prisma
// نوع العقار
enum PropertyType {
  APARTMENT     // شقة
  VILLA         // فيلا
  STUDIO        // استوديو
  DUPLEX        // دوبلكس
  PENTHOUSE     // بنتهاوس
  CHALET        // شاليه
  OTHER         // أخرى
}

// نوع دراسة الجدوى
enum StudyRequestType {
  WITHOUT_FIELD_VISIT   // بدون نزول ميداني
  WITH_FIELD_VISIT      // مع نزول ميداني
}

// حالة طلب دراسة الجدوى
enum FeasibilityRequestStatus {
  PENDING       // في الانتظار
  IN_PROGRESS   // قيد المعالجة
  COMPLETED     // مكتمل
  REJECTED      // مرفوض
}

// نموذج طلب دراسة الجدوى
model FeasibilityRequest {
  id              String                    @id @default(cuid())
  
  // نوع الدراسة
  studyType       StudyRequestType          @map("study_type")
  
  // بيانات العميل
  firstName       String                    @map("first_name")
  lastName        String                    @map("last_name")
  phone           String
  phoneVerified   Boolean                   @default(false) @map("phone_verified")
  
  // بيانات العقار
  propertyType    PropertyType              @map("property_type")
  bedrooms        Int                       @default(1)
  livingRooms     Int                       @default(1) @map("living_rooms")
  kitchens        Int                       @default(1)
  bathrooms       Int                       @default(1)
  
  // بيانات الموقع
  regionName      String                    @map("region_name")      // اسم المنطقة
  districtName    String                    @map("district_name")    // اسم الحي
  streetName      String                    @map("street_name")      // اسم الشارع
  buildingNumber  String                    @map("building_number")  // رقم العمارة
  googleMapsUrl   String?                   @map("google_maps_url")  // رابط جوجل ماب
  latitude        Float?                    // خط العرض (من رابط جوجل ماب)
  longitude       Float?                    // خط الطول (من رابط جوجل ماب)
  
  // الحالة
  status          FeasibilityRequestStatus  @default(PENDING)
  
  // ملاحظات الأدمن
  adminNotes      String?                   @map("admin_notes")
  
  // العلاقة مع دراسة الجدوى (عند إنشائها)
  feasibilityStudy   FeasibilityStudy?      @relation("RequestStudy")
  feasibilityStudyId String?                @unique @map("feasibility_study_id")
  
  createdAt       DateTime                  @default(now()) @map("created_at")
  updatedAt       DateTime                  @updatedAt @map("updated_at")

  @@map("feasibility_requests")
}

// تحديث موديل FeasibilityStudy للربط
// أضف هذا العلاقة في FeasibilityStudy:
// feasibilityRequest FeasibilityRequest? @relation("RequestStudy", fields: [feasibilityRequestId], references: [id])
// feasibilityRequestId String? @unique @map("feasibility_request_id")
```

### 1.2 إنشاء موديل لرموز OTP

```prisma
// رموز التحقق من الهاتف
model PhoneVerification {
  id          String    @id @default(cuid())
  phone       String
  code        String    // رمز التحقق (6 أرقام)
  expiresAt   DateTime  @map("expires_at")
  verified    Boolean   @default(false)
  attempts    Int       @default(0) // عدد محاولات التحقق
  createdAt   DateTime  @default(now()) @map("created_at")

  @@index([phone, code])
  @@map("phone_verifications")
}
```

**الإجراءات:**
- [ ] تحديث `prisma/schema.prisma`
- [ ] تشغيل `npx prisma migrate dev --name add_feasibility_request`

---

## 🎨 المرحلة 2: إنشاء صفحة طلب دراسة الجدوى (الأولوية: عالية)

### 2.1 إنشاء الصفحة الجديدة

**الملف:** `app/feasibility-request/page.tsx`

**التصميم:**
- استخدام نفس نمط الألوان والظلال من الموقع
- `rounded-2xl` للبطاقات والحقول
- تأثيرات Framer Motion
- خريطة Leaflet لعرض الموقع (إذا تم إدخال رابط جوجل ماب)

**هيكل الصفحة - فورم على مرحلتين داخل ودجت واحد:**
```
┌─────────────────────────────────────────────────────────────────┐
│                         Navbar                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │              📋 طلب دراسة جدوى                          │   │
│   │                                                         │   │
│   │  ┌─────────────────────────────────────────────────┐   │   │
│   │  │    ● ━━━━━━━━━━━━━━━━━ ○                        │   │   │
│   │  │  الخطوة 1 من 2        الخطوة 2 من 2             │   │   │
│   │  │  البيانات الأساسية    بيانات الشقة والتحقق      │   │   │
│   │  └─────────────────────────────────────────────────┘   │   │
│   │                                                         │   │
│   │  ╔═══════════════════════════════════════════════════╗ │   │
│   │  ║                                                   ║ │   │
│   │  ║           📝 الخطوة 1: البيانات الأساسية         ║ │   │
│   │  ║                                                   ║ │   │
│   │  ║  ┌─────────────────┐  ┌─────────────────┐        ║ │   │
│   │  ║  │  الاسم الأول   │  │  الاسم الأخير  │        ║ │   │
│   │  ║  └─────────────────┘  └─────────────────┘        ║ │   │
│   │  ║                                                   ║ │   │
│   │  ║  ┌─────────────────────────────────────────┐     ║ │   │
│   │  ║  │  نوع العقار ▼                           │     ║ │   │
│   │  ║  │  (شقة / فيلا / استوديو / ...)          │     ║ │   │
│   │  ║  └─────────────────────────────────────────┘     ║ │   │
│   │  ║                                                   ║ │   │
│   │  ║  ┌─────────────────┐  ┌─────────────────┐        ║ │   │
│   │  ║  │  اسم المنطقة   │  │  اسم الحي       │        ║ │   │
│   │  ║  └─────────────────┘  └─────────────────┘        ║ │   │
│   │  ║                                                   ║ │   │
│   │  ║  ┌─────────────────┐  ┌─────────────────┐        ║ │   │
│   │  ║  │  اسم الشارع    │  │  رقم العمارة    │        ║ │   │
│   │  ║  └─────────────────┘  └─────────────────┘        ║ │   │
│   │  ║                                                   ║ │   │
│   │  ║                         ┌─────────────────┐      ║ │   │
│   │  ║                         │   التالي ←     │      ║ │   │
│   │  ║                         └─────────────────┘      ║ │   │
│   │  ║                                                   ║ │   │
│   │  ╚═══════════════════════════════════════════════════╝ │   │
│   │                                                         │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**الخطوة 2 (بعد الضغط على "التالي"):**
```
│   │  ╔═══════════════════════════════════════════════════╗ │   │
│   │  ║                                                   ║ │   │
│   │  ║         🏠 الخطوة 2: بيانات الشقة والتحقق        ║ │   │
│   │  ║                                                   ║ │   │
│   │  ║  ┌───────────────────────────────────────────┐   ║ │   │
│   │  ║  │              تكوين الشقة                  │   ║ │   │
│   │  ║  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───┐│   ║ │   │
│   │  ║  │  │ 🛏️ غرف │ │ 🛋️ صالات│ │ 🍳 مطابخ│ │🚿│ │   ║ │   │
│   │  ║  │  │  [-]2[+]│ │  [-]1[+]│ │  [-]1[+]│ │  1│ │   ║ │   │
│   │  ║  │  └─────────┘ └─────────┘ └─────────┘ └───┘│   ║ │   │
│   │  ║  └───────────────────────────────────────────┘   ║ │   │
│   │  ║                                                   ║ │   │
│   │  ║  ┌───────────────────────────────────────────┐   ║ │   │
│   │  ║  │  رابط جوجل ماب (اختياري)                 │   ║ │   │
│   │  ║  │  https://maps.google.com/...              │   ║ │   │
│   │  ║  └───────────────────────────────────────────┘   ║ │   │
│   │  ║                                                   ║ │   │
│   │  ║  ┌───────────────────────────────────────────┐   ║ │   │
│   │  ║  │         📍 معاينة الموقع                  │   ║ │   │
│   │  ║  │  ┌─────────────────────────────────────┐ │   ║ │   │
│   │  ║  │  │           [خريطة Leaflet]           │ │   ║ │   │
│   │  ║  │  │              📍                     │ │   ║ │   │
│   │  ║  │  └─────────────────────────────────────┘ │   ║ │   │
│   │  ║  └───────────────────────────────────────────┘   ║ │   │
│   │  ║                                                   ║ │   │
│   │  ║  ┌───────────────────────────────────────────┐   ║ │   │
│   │  ║  │           📱 التحقق من رقم الهاتف        │   ║ │   │
│   │  ║  │                                           │   ║ │   │
│   │  ║  │  ┌─────────────────┐ ┌─────────────────┐ │   ║ │   │
│   │  ║  │  │  رقم الهاتف    │ │ إرسال رمز التحقق│ │   ║ │   │
│   │  ║  │  │  01xxxxxxxxx   │ │       📤        │ │   ║ │   │
│   │  ║  │  └─────────────────┘ └─────────────────┘ │   ║ │   │
│   │  ║  │                                           │   ║ │   │
│   │  ║  │  ┌─────────────────────────────────────┐ │   ║ │   │
│   │  ║  │  │  رمز التحقق: [_][_][_][_][_][_]    │ │   ║ │   │
│   │  ║  │  └─────────────────────────────────────┘ │   ║ │   │
│   │  ║  │                                           │   ║ │   │
│   │  ║  │  ✅ تم التحقق بنجاح                      │   ║ │   │
│   │  ║  └───────────────────────────────────────────┘   ║ │   │
│   │  ║                                                   ║ │   │
│   │  ║  ┌─────────────────┐    ┌─────────────────────┐  ║ │   │
│   │  ║  │   → السابق     │    │ إرسال طلب الدراسة ✓│  ║ │   │
│   │  ║  └─────────────────┘    └─────────────────────┘  ║ │   │
│   │  ║                                                   ║ │   │
│   │  ╚═══════════════════════════════════════════════════╝ │   │
```

### 2.1.1 تفاصيل الخطوات

#### الخطوة 1: البيانات الأساسية
| الحقل | النوع | مطلوب |
|-------|------|-------|
| الاسم الأول | نص | ✅ |
| الاسم الأخير | نص | ✅ |
| نوع العقار | اختيار (شقة/فيلا/استوديو/...) | ✅ |
| اسم المنطقة | نص | ✅ |
| اسم الحي | نص | ✅ |
| اسم الشارع | نص | ✅ |
| رقم العمارة | نص | ✅ |

#### الخطوة 2: بيانات الشقة والتحقق
| الحقل | النوع | مطلوب |
|-------|------|-------|
| عدد غرف النوم | رقم (عداد) | ✅ |
| عدد الصالات | رقم (عداد) | ✅ |
| عدد المطابخ | رقم (عداد) | ✅ |
| عدد الحمامات | رقم (عداد) | ✅ |
| رابط جوجل ماب | رابط URL | ❌ |
| رقم الهاتف | هاتف مصري | ✅ |
| رمز التحقق OTP | 6 أرقام | ✅ |

### 2.1.2 تصميم مؤشر التقدم (Stepper)

```typescript
// مؤشر الخطوات
interface Step {
  id: number;
  title: string;
  description: string;
  icon: React.ReactNode;
}

const steps: Step[] = [
  { id: 1, title: 'البيانات الأساسية', description: 'الاسم والموقع', icon: <User /> },
  { id: 2, title: 'بيانات الشقة والتحقق', description: 'الغرف ورقم الهاتف', icon: <Home /> },
];
```

### 2.1.3 الانتقال بين الخطوات

```typescript
// منطق الانتقال
const [currentStep, setCurrentStep] = useState(1);

// التحقق قبل الانتقال للخطوة التالية
const handleNext = () => {
  if (currentStep === 1) {
    // التحقق من صحة بيانات الخطوة 1
    const isValid = validateStep1();
    if (isValid) {
      setCurrentStep(2);
    }
  }
};

// العودة للخطوة السابقة
const handleBack = () => {
  if (currentStep > 1) {
    setCurrentStep(currentStep - 1);
  }
};

// إرسال الطلب (فقط في الخطوة 2 بعد التحقق)
const handleSubmit = async () => {
  if (!isPhoneVerified) {
    showError('يجب التحقق من رقم الهاتف أولاً');
    return;
  }
  // إرسال الطلب
};
```

### 2.2 شاشة اختيار نوع الدراسة (عند الوصول من Navbar)

**المكون:** `components/feasibility-request/StudyTypeSelector.tsx`

**التصميم:**
```
┌─────────────────────────────────────────────────────────────┐
│              اختر نوع دراسة الجدوى                          │
│                                                             │
│   ┌──────────────────────────┐  ┌──────────────────────────┐│
│   │         📊                │  │         🔍                ││
│   │   بدون نزول ميداني       │  │   مع نزول ميداني         ││
│   │                          │  │                          ││
│   │  ✓ تحليل السوق والمنطقة │  │  ✓ كل ما في الباقة الأولى││
│   │  ✓ دراسة المنافسين      │  │  ✓ زيارة ميدانية للشقة   ││
│   │  ✓ توقعات الإيرادات     │  │  ✓ تجهيزات الشقة الكاملة ││
│   │  ✓ خريطة الموقع         │  │  ✓ تكاليف الأثاث التفصيلية││
│   │  ✓ الشقق المجاورة       │  │  ✓ ملخص التكاليف الإجمالي││
│   │                          │  │                          ││
│   │        [X,XXX جنيه]      │  │        [X,XXX جنيه]      ││
│   │                          │  │                          ││
│   │     [اختر هذه الباقة]   │  │     [اختر هذه الباقة]   ││
│   └──────────────────────────┘  └──────────────────────────┘│
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**المنطق:**
- إذا كان المستخدم قادم من الـ Navbar → عرض شاشة الاختيار أولاً
- إذا كان المستخدم قادم من قسم الخدمات → عرض الفورم مباشرة مع النوع المحدد
- استخدام URL parameter: `/feasibility-request?type=with-field` أو `?type=without-field`

### 2.3 إنشاء المكونات الفرعية

**الملفات:**
- `components/feasibility-request/StudyTypeSelector.tsx` - اختيار نوع الدراسة
- `components/feasibility-request/FeasibilityRequestForm.tsx` - الفورم الرئيسي (multi-step)
- `components/feasibility-request/FormStepper.tsx` - مؤشر التقدم بين الخطوات
- `components/feasibility-request/Step1BasicInfo.tsx` - الخطوة 1: البيانات الأساسية
- `components/feasibility-request/Step2PropertyDetails.tsx` - الخطوة 2: بيانات الشقة والتحقق
- `components/feasibility-request/PropertyTypeSelector.tsx` - اختيار نوع العقار
- `components/feasibility-request/RoomCounters.tsx` - عدادات الغرف
- `components/feasibility-request/LocationFields.tsx` - حقول الموقع
- `components/feasibility-request/MapPreview.tsx` - معاينة الخريطة
- `components/feasibility-request/PhoneVerification.tsx` - التحقق من الهاتف
- `components/feasibility-request/OTPInput.tsx` - حقول إدخال رمز OTP (6 خانات)

**الإجراءات:**
- [ ] إنشاء مجلد `app/feasibility-request/`
- [ ] إنشاء `page.tsx`
- [ ] إنشاء مجلد `components/feasibility-request/`
- [ ] إنشاء المكونات الفرعية
- [ ] إنشاء مكون الـ Stepper مع أنيميشن الانتقال

---

## 🎯 المرحلة 2.5: تحديث قسم الخدمات (الأولوية: عالية)

### 2.5.1 إزالة الخدمات المؤقتة الحالية

**الملف:** `components/ServicesSection.tsx`

**الخدمات الحالية (للإزالة):**
1. استشارة إعداد وحدة جديدة
2. برنامج إدارة العمليات الكاملة
3. دورة استضافة Airbnb للمبتدئين
4. تدريب الفرق الميدانية
5. استشارة التسعير والأداء
6. حزمة الإدارة الكاملة

### 2.5.2 إضافة بطاقات دراسات الجدوى

**الخدمات الجديدة (بطاقتين فقط):**

```typescript
const services: Service[] = [
  {
    id: 1,
    image: '/images/services/feasibility-basic.jpg',
    badge: 'الأكثر طلباً',
    badgeVariant: 'info',
    title: 'دراسة جدوى بدون نزول ميداني',
    description: 'تحليل شامل للسوق والمنطقة وتوقعات الإيرادات بدون الحاجة لزيارة الشقة',
    price: 'X,XXX جنيه',
    buttonText: 'اطلب الآن',
    features: [
      'تحليل السوق والمنطقة',
      'دراسة المنافسين والأسعار',
      'توقعات الإيرادات',
      'خريطة الموقع والشقق المجاورة',
    ],
    href: '/feasibility-request?type=without-field',
  },
  {
    id: 2,
    image: '/images/services/feasibility-full.jpg',
    badge: 'شامل',
    badgeVariant: 'success',
    title: 'دراسة جدوى مع نزول ميداني',
    description: 'دراسة شاملة تتضمن زيارة ميدانية للشقة وخطة تجهيز كاملة مع التكاليف',
    price: 'X,XXX جنيه',
    buttonText: 'اطلب الآن',
    features: [
      'كل ما في الباقة الأساسية',
      'زيارة ميدانية للشقة',
      'خطة تجهيز الأثاث والمستلزمات',
      'تكاليف تفصيلية لكل غرفة',
      'ملخص التكلفة الإجمالية',
    ],
    href: '/feasibility-request?type=with-field',
  },
];
```

### 2.5.3 تحديث تصميم البطاقة

**تحديثات التصميم:**
- عرض قائمة المميزات في كل بطاقة
- الزر يوجه مباشرة للفورم مع تحديد النوع
- استخدام `Link` من Next.js بدلاً من `onClick`

**الإجراءات:**
- [ ] تحديث `components/ServicesSection.tsx`
- [ ] إضافة صور جديدة للخدمات في `/public/images/services/`
- [ ] تحديث واجهة `Service` لتشمل `features` و `href`

---

## 🔗 المرحلة 3: تحديث الـ Navbar (الأولوية: عالية)

### 3.1 إضافة زر "طلب دراسة جدوى"

**الملف:** `components/Navbar.tsx`

**التعديلات:**
1. إضافة رابط جديد في `navLinks`:
```typescript
const navLinks = [
  { label: 'الرئيسية', href: '#home' },
  { label: 'من أنا', href: '#about' },
  { label: 'الخدمات', href: '#services' },
  { label: 'المحتوى', href: '#content' },
  { label: 'دراسة الجدوى', href: '/feasibility-request', isPage: true }, // جديد
  { label: 'تواصل معي', href: '#contact' },
];
```

2. تحديث دالة `scrollToSection` للتعامل مع الصفحات:
```typescript
const handleNavClick = (link: NavLink) => {
  if (link.isPage) {
    router.push(link.href);
  } else {
    scrollToSection(link.href);
  }
  setIsMobileMenuOpen(false);
};
```

**الإجراءات:**
- [ ] تحديث `components/Navbar.tsx`
- [ ] إضافة `useRouter` من `next/navigation`
- [ ] تحديث روابط Desktop والـ Mobile menu

---

## 📡 المرحلة 4: إنشاء APIs (الأولوية: عالية)

### 4.1 API لإرسال رمز التحقق

**الملف:** `app/api/phone-verification/send/route.ts`

```typescript
// POST - إرسال رمز التحقق
// يستقبل: { phone: string }
// يرسل: رمز 6 أرقام عبر SMS (أو واتساب)
// يحفظ الرمز في قاعدة البيانات مع صلاحية 10 دقائق
```

### 4.2 API للتحقق من الرمز

**الملف:** `app/api/phone-verification/verify/route.ts`

```typescript
// POST - التحقق من الرمز
// يستقبل: { phone: string, code: string }
// يتحقق من صحة الرمز
// يحدد عدد المحاولات (3 محاولات كحد أقصى)
```

### 4.3 API لإنشاء طلب دراسة الجدوى

**الملف:** `app/api/feasibility-request/route.ts`

```typescript
// POST - إنشاء طلب جديد
// يتحقق من أن الهاتف تم التحقق منه
// يحفظ الطلب في قاعدة البيانات
// يرسل إشعار للأدمن (اختياري)

// GET - جلب الطلبات (للأدمن فقط)
```

### 4.4 إضافة Validation Schema

**الملف:** `lib/validations/feasibility-request.ts`

```typescript
import { z } from 'zod';

export const feasibilityRequestSchema = z.object({
  firstName: z.string().min(2, 'الاسم الأول مطلوب'),
  lastName: z.string().min(2, 'الاسم الأخير مطلوب'),
  phone: z.string()
    .min(10, 'رقم الهاتف غير صالح')
    .regex(/^(\+?20)?01[0125][0-9]{8}$/, 'رقم هاتف مصري غير صالح'),
  propertyType: z.enum(['APARTMENT', 'VILLA', 'STUDIO', 'DUPLEX', 'PENTHOUSE', 'CHALET', 'OTHER']),
  bedrooms: z.number().min(0).max(10),
  livingRooms: z.number().min(0).max(5),
  kitchens: z.number().min(0).max(3),
  bathrooms: z.number().min(0).max(5),
  regionName: z.string().min(2, 'اسم المنطقة مطلوب'),
  districtName: z.string().min(2, 'اسم الحي مطلوب'),
  streetName: z.string().min(2, 'اسم الشارع مطلوب'),
  buildingNumber: z.string().min(1, 'رقم العمارة مطلوب'),
  googleMapsUrl: z.string().url().optional().or(z.literal('')),
});

export const phoneVerificationSchema = z.object({
  phone: z.string()
    .min(10, 'رقم الهاتف غير صالح')
    .regex(/^(\+?20)?01[0125][0-9]{8}$/, 'رقم هاتف مصري غير صالح'),
});

export const verifyCodeSchema = z.object({
  phone: z.string(),
  code: z.string().length(6, 'رمز التحقق يجب أن يكون 6 أرقام'),
});
```

**الإجراءات:**
- [ ] إنشاء `app/api/phone-verification/send/route.ts`
- [ ] إنشاء `app/api/phone-verification/verify/route.ts`
- [ ] إنشاء `app/api/feasibility-request/route.ts`
- [ ] إنشاء `lib/validations/feasibility-request.ts`

---

## 📱 المرحلة 5: خدمة SMS/OTP (الأولوية: متوسطة-عالية)

### 5.1 اختيار مزود SMS

**الخيارات المتاحة:**
1. **Twilio** - الأكثر شهرة، يدعم مصر
2. **Vonage (Nexmo)** - بديل جيد
3. **MessageBird** - يدعم المنطقة العربية
4. **Unifonic** - متخصص في الشرق الأوسط

### 5.2 إنشاء خدمة SMS

**الملف:** `lib/sms-service.ts`

```typescript
// دالة إرسال رسالة SMS
export async function sendSMS(phone: string, message: string): Promise<boolean> {
  // Implementation based on chosen provider
}

// دالة إنشاء رمز عشوائي
export function generateOTPCode(): string {
  return Math.floor(100000 + Math.random() * 900000).toString();
}
```

### 5.3 متغيرات البيئة المطلوبة

**الملف:** `.env.local`

```env
# SMS Provider (Twilio example)
TWILIO_ACCOUNT_SID=your_account_sid
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_PHONE_NUMBER=+1234567890
```

**الإجراءات:**
- [ ] اختيار مزود SMS
- [ ] إنشاء حساب وتفعيله
- [ ] إنشاء `lib/sms-service.ts`
- [ ] إضافة متغيرات البيئة

---

## 🛠️ المرحلة 6: تحديث قسم CTA (الأولوية: متوسطة)

### 6.1 تبسيط فورم التواصل

**الملف:** `components/CTASection.tsx`

**التعديلات:**
1. إزالة خيار "أضف تفاصيل شقتك"
2. إزالة `roomConfig` state
3. إزالة `RoomCounter` component
4. إزالة الـ imports غير المستخدمة

**الحقول المتبقية:**
- الاسم الأول
- الاسم الأخير
- البريد الإلكتروني
- الرسالة

**الإجراءات:**
- [ ] تحديث `components/CTASection.tsx`
- [ ] إزالة المكونات غير المستخدمة
- [ ] تحديث API `/api/consultations` لإزالة room config

---

## 👨‍💼 المرحلة 7: صفحة الأدمن لطلبات دراسات الجدوى (الأولوية: متوسطة)

### 7.1 إنشاء/تحديث صفحة الأدمن

**الملف:** `app/admin/feasibility-requests/page.tsx`

**الميزات:**
- عرض جميع الطلبات مع الفلترة والبحث
- عرض تفاصيل كل طلب
- عرض الموقع على الخريطة
- تغيير حالة الطلب
- إنشاء دراسة جدوى من الطلب مباشرة

### 7.2 تحديث Sidebar

**الملف:** `components/admin/Sidebar.tsx`

**إضافة رابط جديد:**
```typescript
{ label: 'طلبات الدراسات', href: '/admin/feasibility-requests', icon: FileText }
```

**الإجراءات:**
- [ ] إنشاء `app/admin/feasibility-requests/page.tsx`
- [ ] تحديث `components/admin/Sidebar.tsx`
- [ ] إنشاء API للأدمن

---

## 🗺️ المرحلة 8: تكامل الخريطة (الأولوية: متوسطة)

### 8.1 استخراج الإحداثيات من رابط جوجل ماب

**الملف:** `lib/maps-utils.ts`

```typescript
// استخراج lat/lng من رابط جوجل ماب
export function extractCoordsFromGoogleMapsUrl(url: string): {
  latitude: number;
  longitude: number;
} | null {
  // أمثلة على الروابط:
  // https://maps.google.com/?q=30.0444,31.2357
  // https://www.google.com/maps/@30.0444,31.2357,17z
  // https://goo.gl/maps/xxxxx
}
```

### 8.2 عرض الدبوس على الخريطة

**المكون:** `components/feasibility-request/MapPreview.tsx`

- استخدام Leaflet (موجود بالفعل في المشروع)
- عرض دبوس عند استخراج الإحداثيات بنجاح

**الإجراءات:**
- [ ] إنشاء `lib/maps-utils.ts`
- [ ] إنشاء `components/feasibility-request/MapPreview.tsx`
- [ ] تحديث الفورم لعرض الخريطة

---

## 🔐 المرحلة 9: التحقق من رقم الهاتف للتسجيل (الأولوية: منخفضة - آخر مرحلة)

### 9.1 تحديث نظام التسجيل

**الملفات:**
- `components/auth/RegisterForm.tsx`
- `lib/validations/auth.ts`
- `app/api/auth/register/route.ts`

**التعديلات:**
1. إضافة حقل رقم الهاتف
2. إضافة زر "أرسل رمز التحقق"
3. إضافة حقل رمز التحقق
4. عدم السماح بالتسجيل إلا بعد التحقق

### 9.2 تحديث قاعدة البيانات

```prisma
model User {
  // ... existing fields
  phone         String?   @unique
  phoneVerified Boolean   @default(false) @map("phone_verified")
}
```

**الإجراءات:**
- [ ] تحديث `prisma/schema.prisma`
- [ ] تشغيل migration
- [ ] تحديث `RegisterForm.tsx`
- [ ] تحديث `auth.ts` validation
- [ ] تحديث API التسجيل

---

## 📋 ملخص المراحل والأولويات

| المرحلة | الوصف | الأولوية | الحالة |
|---------|-------|----------|--------|
| 1 | تحديث قاعدة البيانات (موديلات جديدة) | عالية جداً | ⏳ |
| 2 | صفحة طلب دراسة الجدوى + شاشة الاختيار | عالية | ⏳ |
| 2.5 | تحديث قسم الخدمات (بطاقتين فقط) | عالية | ⏳ |
| 3 | تحديث Navbar (زر جديد) | عالية | ⏳ |
| 4 | إنشاء APIs (التحقق + الطلبات) | عالية | ⏳ |
| 5 | خدمة SMS/OTP | متوسطة-عالية | ⏳ |
| 6 | تحديث قسم CTA (تبسيط الفورم) | متوسطة | ⏳ |
| 7 | صفحة الأدمن لطلبات الدراسات | متوسطة | ⏳ |
| 7.5 | تعديل محرر دراسة الجدوى (نوعين) | متوسطة | ⏳ |
| 8 | تكامل الخريطة | متوسطة | ⏳ |
| 9 | التحقق من الهاتف للتسجيل | منخفضة | ⏳ |

---

## 🛠️ المرحلة 7.5: تعديل محرر دراسة الجدوى (الأولوية: متوسطة)

### 7.5.1 تعديل إنشاء الدراسة حسب النوع

**الملف:** `hooks/useSlides.ts`

**المنطق:**
عند إنشاء دراسة جدوى من طلب عميل، يتم التحقق من نوع الطلب:

#### إذا كان النوع: بدون نزول ميداني (`WITHOUT_FIELD_VISIT`)
```typescript
// الشرائح المُنشأة تلقائياً:
const slidesWithoutField = [
  'cover',           // الغلاف
  'introduction',    // المقدمة
  'room-setup',      // تكوين الشقة (عدد الغرف فقط)
  'area-study',      // دراسة المنطقة
  'map',             // الخريطة
  'nearby-apartments', // الشقق المجاورة
  'statistics',      // الإحصائيات
  'footer',          // الخاتمة
];

// ملاحظة: لا يتم إنشاء:
// - bedroom, living-room, kitchen, bathroom (شرائح الغرف التفصيلية)
// - cost-summary (ملخص التكاليف)
```

#### إذا كان النوع: مع نزول ميداني (`WITH_FIELD_VISIT`)
```typescript
// الشرائح المُنشأة تلقائياً (النظام الحالي بدون تغيير):
const slidesWithField = [
  'cover',           // الغلاف
  'introduction',    // المقدمة
  'room-setup',      // تكوين الشقة
  'bedroom',         // غرف النوم (حسب العدد)
  'living-room',     // الصالات (حسب العدد)
  'kitchen',         // المطابخ (حسب العدد)
  'bathroom',        // الحمامات (حسب العدد)
  'cost-summary',    // ملخص التكاليف
  'area-study',      // دراسة المنطقة
  'map',             // الخريطة
  'nearby-apartments', // الشقق المجاورة
  'statistics',      // الإحصائيات
  'footer',          // الخاتمة
];
```

### 7.5.2 تحديث موديل FeasibilityStudy

**إضافة حقل لنوع الدراسة:**
```prisma
model FeasibilityStudy {
  // ... existing fields
  studyType   StudyRequestType  @default(WITH_FIELD_VISIT) @map("study_type")
}
```

### 7.5.3 تحديث الإحصائيات والملخص

**للدراسات بدون نزول ميداني:**
- إخفاء "التكلفة الإجمالية" من الإحصائيات
- إخفاء قسم ملخص التكاليف
- التركيز على إيرادات المنطقة والمنافسين

**الإجراءات:**
- [ ] تحديث `hooks/useSlides.ts` - دالة `generateRoomSlides`
- [ ] تحديث `prisma/schema.prisma` - إضافة `studyType` للـ FeasibilityStudy
- [ ] تحديث المحرر لإخفاء عناصر التكلفة حسب النوع
- [ ] تحديث العارض (Viewer) لعرض المحتوى حسب النوع

---

## 🔧 الموارد الموجودة في المشروع

### ✅ موجود ويمكن استخدامه:
- `Leaflet` و `react-leaflet` - للخرائط
- `zod` - للتحقق من البيانات
- `@prisma/client` - لقاعدة البيانات
- `rate-limit.ts` - للحد من المحاولات
- `security-headers.ts` - للأمان
- مكونات UI (`Button`, `Input`, `Card`, etc.)
- أنماط Framer Motion
- نظام الألوان والتصميم
- محرر دراسة الجدوى الموجود

### ❌ يحتاج إنشاء:
- موديل `FeasibilityRequest` في Prisma
- موديل `PhoneVerification` في Prisma
- خدمة SMS
- صفحة `/feasibility-request`
- مكون `StudyTypeSelector`
- APIs للتحقق والطلبات
- صفحة أدمن للطلبات الجديدة
- تحديث قسم الخدمات (بطاقتين جديدتين)

---

## 📝 ملاحظات إضافية

1. **الأمان:**
   - Rate limiting على API التحقق (3 محاولات كحد أقصى)
   - Rate limiting على إرسال SMS (رسالة واحدة كل دقيقتين)
   - تشفير رموز التحقق في قاعدة البيانات

2. **تجربة المستخدم:**
   - عرض تقدم الفورم (خطوات واضحة)
   - رسائل خطأ واضحة بالعربي
   - تأكيد عند إرسال الطلب بنجاح
   - شاشة اختيار واضحة بين النوعين

3. **الخريطة:**
   - عرض خريطة تفاعلية للموقع
   - إمكانية تعديل موقع الدبوس يدوياً (اختياري)

4. **SMS:**
   - استخدام رسائل واتساب كبديل (أرخص وأسهل)
   - أو استخدام Twilio Verify للحلول الجاهزة

5. **قسم الخدمات:**
   - بطاقتين فقط (بدون/مع نزول ميداني)
   - كل بطاقة توجه مباشرة للفورم مع تحديد النوع
   - عرض مميزات كل باقة بوضوح

6. **نوعي الدراسة:**
   - **بدون نزول ميداني**: دراسة السوق والمنطقة فقط (سعر أقل)
   - **مع نزول ميداني**: دراسة شاملة + تجهيزات الشقة (سعر أعلى)

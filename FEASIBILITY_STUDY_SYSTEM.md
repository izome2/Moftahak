# 📋 نظام دراسات الجدوى - Moftahak Feasibility Study System

## 🎯 نظرة عامة على المشروع

إنشاء نظام متكامل لتصميم وإنشاء دراسات الجدوى للعقارات، يشبه Figma في سهولة الاستخدام ولكن مخصص لإنشاء وثائق دراسات الجدوى بنظام السحب والإفلات (Drag & Drop).

---

## 📌 ملخص المتطلبات (Brief)

### سيناريو الاستخدام
```
العميل يطلب استشارة ← الأدمن يستقبل الطلب في Dashboard ← 
الأدمن ينقر "إنشاء دراسة جدوى" ← يدخل محرر الدراسة ← 
يصمم الدراسة كاملة ← ينقر "إرسال" ← 
النظام ينشئ رابط خاص + يرسله لإيميل العميل
```

### المخرج النهائي
**وثيقة دراسة جدوى تفاعلية** تحتوي على:

| القسم | المحتوى | النوع |
|-------|---------|-------|
| 🎨 **الغلاف** | شعار + اسم العميل + توقيع مفتاحك | ثابت + اسم متغير |
| 📝 **المقدمة** | ترحيب + تعريف بالدراسة | ثابت + نص متغير |
| 🏠 **تجهيزات الشقة** | غرف + مطبخ + صالة + حمامات + تكلفة كل قسم | ديناميكي (سحب وإفلات) |
| 🗺️ **دراسة المنطقة** | خريطة + 10 شقق محيطة + إحصائيات | ديناميكي + متغير |
| 📊 **الملخص** | رسوم بيانية + إحصائيات + توصيات | ديناميكي |
| 📞 **الخاتمة** | شعار + وسائل تواصل | ثابت |

---

## 🔄 تصنيف آلية العمل

### 1️⃣ العناصر الثابتة (Static) - لا تتغير بين الدراسات
```
├── شريحة الغلاف (التصميم والألوان)
├── شعار مفتاحك
├── نص "تم إنشاء هذا النموذج من خلال مفتاحك"
├── المقدمة الترحيبية (الهيكل)
├── شريحة الخاتمة (الفوتر)
└── وسائل التواصل والعناوين
```

### 2️⃣ العناصر المتغيرة (Editable) - قابلة للتحرير بحرية
```
├── اسم العميل
├── جميع النصوص والعناوين
├── أسعار العناصر
├── وصف كل غرفة
├── معلومات الشقق المحيطة
├── التوصيات والملاحظات
└── أي نص يظهر في الدراسة
```

### 3️⃣ العناصر الديناميكية (Dynamic) - سحب وإفلات مع قيود ذكية
```
├── عناصر الغرف (أثاث، أجهزة)
├── أدوات المطبخ
├── مكونات الحمام
├── مكونات الصالة
├── الصور المربعة (يمين/يسار)
├── دبابيس الخريطة (10 كحد أقصى)
└── توزيع العناصر داخل كل قسم

قواعد التموضع:
✓ العناصر تبقى داخل قسمها المخصص فقط
✓ Snap to Grid - التموضع التلقائي لأقرب مكان صحيح
✓ تصميم Responsive يتكيف مع الحجم
✓ لا يمكن نقل عنصر من قسم لآخر
```

---

## 📑 جدول المحتويات

1. [هيكل النظام](#-هيكل-النظام)
2. [المرحلة الأولى: البنية التحتية](#-المرحلة-الأولى-البنية-التحتية-وقاعدة-البيانات)
3. [المرحلة الثانية: نظام الطلبات](#-المرحلة-الثانية-نظام-طلبات-الاستشارات)
4. [المرحلة الثالثة: محرر دراسة الجدوى](#-المرحلة-الثالثة-محرر-دراسة-الجدوى-الرئيسي)
5. [المرحلة الرابعة: أقسام الدراسة](#-المرحلة-الرابعة-أقسام-دراسة-الجدوى)
6. [المرحلة الخامسة: نظام المشاركة](#-المرحلة-الخامسة-نظام-المشاركة-والإرسال)
7. [المرحلة السادسة: التحسينات](#-المرحلة-السادسة-التحسينات-والتجميل)

---

## 🏗 هيكل النظام

### تصنيف العناصر

| النوع | الوصف | أمثلة |
|-------|-------|-------|
| **ثابت (Static)** | عناصر لا تتغير بين الدراسات | المقدمة، الفوتر، الشعار، الترحيب |
| **متغير (Editable)** | نصوص وعناصر قابلة للتحرير | اسم العميل، أسماء العناصر، الأسعار، الوصف |
| **ديناميكي (Dynamic)** | عناصر قابلة للسحب والإفلات مع تموضع ذكي | مكونات الغرف، أدوات المطبخ، الدبابيس على الخريطة |

### هيكل المجلدات المطلوب

```
app/
├── admin/
│   ├── consultations/           # طلبات الاستشارات
│   │   ├── page.tsx             # قائمة الطلبات
│   │   └── [id]/
│   │       └── page.tsx         # تفاصيل طلب
│   └── feasibility/             # محرر دراسة الجدوى
│       ├── page.tsx             # قائمة الدراسات
│       └── [id]/
│           └── page.tsx         # محرر الدراسة
├── study/
│   └── [shareId]/               # صفحة عرض الدراسة للعميل
│       └── page.tsx
└── api/
    ├── consultations/           # API طلبات الاستشارات
    ├── feasibility/             # API دراسات الجدوى
    └── share/                   # API إنشاء روابط المشاركة

components/
├── feasibility/
│   ├── editor/                  # مكونات المحرر
│   │   ├── FeasibilityEditor.tsx
│   │   ├── EditorToolbar.tsx
│   │   ├── EditorSidebar.tsx
│   │   └── DragDropContext.tsx
│   ├── slides/                  # الشرائح
│   │   ├── CoverSlide.tsx       # الغلاف
│   │   ├── IntroSlide.tsx       # المقدمة
│   │   ├── RoomsSlide.tsx       # الغرف
│   │   ├── MapSlide.tsx         # الخريطة
│   │   ├── SummarySlide.tsx     # الملخص
│   │   └── FooterSlide.tsx      # الخاتمة
│   ├── elements/                # العناصر القابلة للسحب
│   │   ├── KitchenItems.tsx
│   │   ├── RoomItems.tsx
│   │   ├── BathroomItems.tsx
│   │   └── LivingRoomItems.tsx
│   ├── shared/                  # عناصر مشتركة
│   │   ├── EditableText.tsx
│   │   ├── ImageUploader.tsx
│   │   ├── PriceCalculator.tsx
│   │   └── MapPinPopup.tsx
│   └── viewer/                  # عارض الدراسة للعميل
│       └── StudyViewer.tsx

lib/
├── feasibility/
│   ├── items-data.ts            # بيانات العناصر (مطبخ، غرف، إلخ)
│   ├── pricing.ts               # حسابات التكلفة
│   └── share-utils.ts           # أدوات المشاركة

types/
└── feasibility.ts               # أنواع TypeScript
```

---

## 🔷 المرحلة الأولى: البنية التحتية وقاعدة البيانات

### الخطوة 1.1: تحديث مخطط Prisma

```prisma
// prisma/schema.prisma - إضافة الجداول الجديدة

// طلبات الاستشارات
model ConsultationRequest {
  id            String   @id @default(cuid())
  clientId      String
  client        User     @relation(fields: [clientId], references: [id])
  propertyType  String   // نوع العقار (شقة، فيلا، إلخ)
  roomCount     Int      // عدد الغرف
  location      String   // الموقع
  message       String   @db.Text
  status        ConsultationStatus @default(PENDING)
  feasibilityId String?  @unique
  feasibility   FeasibilityStudy? @relation(fields: [feasibilityId], references: [id])
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

enum ConsultationStatus {
  PENDING      // قيد الانتظار
  IN_PROGRESS  // جاري العمل
  COMPLETED    // مكتمل
  SENT         // تم الإرسال
}

// دراسة الجدوى
model FeasibilityStudy {
  id            String   @id @default(cuid())
  title         String
  clientName    String
  clientEmail   String
  shareId       String   @unique @default(cuid())
  shareUrl      String?
  content       Json     // محتوى الدراسة بالكامل (الشرائح والعناصر)
  totalCost     Float    @default(0)
  status        StudyStatus @default(DRAFT)
  consultation  ConsultationRequest?
  createdBy     String
  admin         User     @relation(fields: [createdBy], references: [id])
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  sentAt        DateTime?
}

enum StudyStatus {
  DRAFT        // مسودة
  READY        // جاهز للإرسال
  SENT         // تم الإرسال
  VIEWED       // تمت المشاهدة
}

// عناصر المطبخ والغرف (جدول مرجعي)
model FeasibilityItem {
  id            String   @id @default(cuid())
  nameAr        String   // الاسم بالعربية
  nameEn        String   // الاسم بالإنجليزية
  category      ItemCategory
  icon          String   // اسم الأيقونة
  defaultPrice  Float    @default(0)
  isActive      Boolean  @default(true)
}

enum ItemCategory {
  KITCHEN      // أدوات المطبخ
  BEDROOM      // غرفة النوم
  BATHROOM     // الحمام
  LIVING_ROOM  // الصالة
  GENERAL      // عام
}

// الشقق المحيطة للمقارنة
model NearbyProperty {
  id            String   @id @default(cuid())
  studyId       String
  study         FeasibilityStudy @relation(fields: [studyId], references: [id])
  title         String
  price         Float
  roomCount     Int
  features      String   @db.Text
  timesRented   Int      @default(0)
  highestRent   Float    @default(0)
  latitude      Float
  longitude     Float
  pinIndex      Int      // ترتيب الدبوس (1-10)
}
```

### الخطوة 1.2: إنشاء ملفات الأنواع (Types)

**ملف: `types/feasibility.ts`**

```typescript
// أنواع دراسة الجدوى

export interface FeasibilityStudy {
  id: string;
  title: string;
  clientName: string;
  clientEmail: string;
  shareId: string;
  shareUrl?: string;
  content: StudyContent;
  totalCost: number;
  status: StudyStatus;
  createdAt: Date;
  updatedAt: Date;
  sentAt?: Date;
}

export type StudyStatus = 'DRAFT' | 'READY' | 'SENT' | 'VIEWED';

export interface StudyContent {
  slides: Slide[];
  rooms: Room[];
  nearbyProperties: NearbyProperty[];
  settings: StudySettings;
}

export interface Slide {
  id: string;
  type: SlideType;
  order: number;
  content: SlideContent;
}

export type SlideType = 
  | 'cover'           // الغلاف
  | 'intro'           // المقدمة
  | 'rooms'           // الغرف ومكوناتها
  | 'map'             // خريطة المنطقة
  | 'summary'         // الملخص والإحصائيات
  | 'footer';         // الخاتمة

export interface SlideContent {
  title?: string;
  subtitle?: string;
  description?: string;
  elements?: DraggableElement[];
  images?: ImageElement[];
  customData?: Record<string, unknown>;
}

export interface Room {
  id: string;
  type: RoomType;
  name: string;
  number: number;
  items: RoomItem[];
  totalCost: number;
  image?: string;
}

export type RoomType = 'bedroom' | 'bathroom' | 'kitchen' | 'living_room';

export interface RoomItem {
  id: string;
  itemId: string;       // مرجع للعنصر من القائمة
  name: string;
  icon: string;
  price: number;
  quantity: number;
  position: Position;   // موقع العنصر بعد السحب
}

export interface DraggableElement {
  id: string;
  type: string;
  content: string | Record<string, unknown>;
  position: Position;
  size: Size;
  isLocked: boolean;
}

export interface Position {
  x: number;
  y: number;
  zone: string;         // المنطقة المسموح بها
}

export interface Size {
  width: number;
  height: number;
}

export interface ImageElement {
  id: string;
  url: string;
  alt: string;
  position: 'left' | 'right';
  size: Size;
}

export interface NearbyProperty {
  id: string;
  title: string;
  price: number;
  roomCount: number;
  features: string[];
  timesRented: number;
  highestRent: number;
  coordinates: {
    lat: number;
    lng: number;
  };
  pinIndex: number;
}

export interface StudySettings {
  currency: string;
  language: 'ar' | 'en';
  theme: {
    primaryColor: string;
    secondaryColor: string;
  };
}

// عناصر السحب والإفلات
export interface DragItem {
  id: string;
  type: ItemCategory;
  nameAr: string;
  nameEn: string;
  icon: string;
  defaultPrice: number;
}

export type ItemCategory = 
  | 'KITCHEN' 
  | 'BEDROOM' 
  | 'BATHROOM' 
  | 'LIVING_ROOM' 
  | 'GENERAL';
```

### الخطوة 1.3: تنفيذ Migration

```bash
npx prisma migrate dev --name add_feasibility_system
npx prisma generate
```

---

## 🔷 المرحلة الثانية: نظام طلبات الاستشارات

### الخطوة 2.1: API طلبات الاستشارات

**ملف: `app/api/consultations/route.ts`**

```typescript
// GET: جلب جميع الطلبات
// POST: إنشاء طلب جديد (من العميل)
```

**ملف: `app/api/consultations/[id]/route.ts`**

```typescript
// GET: جلب طلب محدد
// PATCH: تحديث حالة الطلب
// DELETE: حذف طلب
```

### الخطوة 2.2: صفحة قائمة الطلبات في لوحة الإدارة

**ملف: `app/admin/consultations/page.tsx`**

```
المتطلبات:
- جدول يعرض جميع طلبات الاستشارات
- أعمدة: اسم العميل، البريد، نوع العقار، الحالة، التاريخ، الإجراءات
- فلترة حسب الحالة (قيد الانتظار، جاري العمل، مكتمل)
- زر "إنشاء دراسة جدوى" لكل طلب
- تصميم متوافق مع هوية الموقع
```

### الخطوة 2.3: مكون بطاقة الطلب

**ملف: `components/admin/ConsultationCard.tsx`**

```
المتطلبات:
- عرض معلومات العميل
- عرض تفاصيل الطلب
- أزرار الإجراءات (عرض، إنشاء دراسة، حذف)
- تأثيرات Framer Motion
```

---

## 🔷 المرحلة الثالثة: محرر دراسة الجدوى الرئيسي

### الخطوة 3.1: إعداد نظام السحب والإفلات

**التقنيات المستخدمة:**
- `@dnd-kit/core` - للسحب والإفلات
- `@dnd-kit/sortable` - للترتيب
- `@dnd-kit/utilities` - أدوات مساعدة

```bash
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```

### الخطوة 3.2: مكون المحرر الرئيسي

**ملف: `components/feasibility/editor/FeasibilityEditor.tsx`**

```
المتطلبات:
- تخطيط ثلاثي الأعمدة:
  ├── الشريط الجانبي الأيمن (قائمة العناصر للسحب)
  ├── منطقة العمل الوسطى (الشرائح)
  └── الشريط الجانبي الأيسر (خصائص العنصر المحدد)
  
- شريط الأدوات العلوي:
  ├── اسم الدراسة (قابل للتحرير)
  ├── أزرار التراجع/الإعادة
  ├── زر المعاينة
  ├── زر الحفظ
  └── زر الإرسال

- إدارة الحالة:
  ├── حالة الشرائح الحالية
  ├── حالة العناصر المسحوبة
  ├── حالة العنصر المحدد
  └── سجل التراجع/الإعادة
```

### الخطوة 3.3: الشريط الجانبي للعناصر

**ملف: `components/feasibility/editor/EditorSidebar.tsx`**

```
المتطلبات:
- تبويبات للفئات:
  ├── أدوات المطبخ (مايكرويف، غلاية، خلاط، ثلاجة، فرن، إلخ)
  ├── غرفة النوم (سرير، دولاب، تسريحة، مكيف، إلخ)
  ├── الحمام (غسالة، سخان، حوض، مرآة، إلخ)
  ├── الصالة (كنبة، تلفزيون، طاولة، ستائر، إلخ)
  └── عام (صور، نصوص، فواصل)

- كل عنصر يعرض:
  ├── أيقونة جرافيك (داخل مربع واضح)
  ├── الاسم
  └── السعر الافتراضي

- العناصر قابلة للسحب إلى منطقة العمل
```

### الخطوة 3.4: منطقة العمل

**ملف: `components/feasibility/editor/EditorWorkspace.tsx`**

```
المتطلبات:
- عرض الشرائح بشكل متسلسل
- كل شريحة لها منطقة إفلات محددة
- التموضع الذكي (Snap to Grid):
  ├── عند إفلات عنصر في مكان غير دقيق
  ├── يتحرك تلقائياً لأقرب موقع صحيح
  └── لا يمكن نقل عنصر خارج قسمه المخصص

- التصميم:
  ├── خلفية بيج مع ظلال
  ├── border-radius للعناصر
  └── تأثيرات hover وselection
```

### الخطوة 3.5: مكون النص القابل للتحرير

**ملف: `components/feasibility/shared/EditableText.tsx`**

```
المتطلبات:
- نقر مزدوج للتحرير
- حفظ تلقائي عند الخروج
- دعم أحجام نص مختلفة
- دعم المحاذاة (يمين، وسط، يسار)
```

---

## 🔷 المرحلة الرابعة: أقسام دراسة الجدوى

### القسم 4.1: شريحة الغلاف (CoverSlide) 🎨

**ملف: `components/feasibility/slides/CoverSlide.tsx`**

```
التصميم:
├── الخلفية: اللون الأخضر الفاتح (#10302b) من هوية الموقع
├── الشعار: مفتاحك - في المنتصف العلوي (أو الأعلى)
├── اسم العميل: خط كبير واضح في الوسط (قابل للتحرير)
├── "دراسة جدوى عقارية": عنوان فرعي أنيق
└── "تم إنشاء هذا النموذج من خلال مفتاحك": أسفل الصفحة بخط صغير

النوع: ثابت مع نص متغير (اسم العميل فقط)

تفاصيل التصميم:
├── الخط: Dubai Bold للعربية
├── اللون الرئيسي: أخضر داكن (#10302b)
├── لون النص: أبيض/ذهبي (#edbf8c)
├── بدون border-radius (صفحة كاملة)
└── تأثير gradient خفيف اختياري
```

### القسم 4.2: شريحة المقدمة (IntroSlide) 📝

**ملف: `components/feasibility/slides/IntroSlide.tsx`**

```
التصميم:
├── الخلفية: درجات البيج الفاتح (#f5ebe0) مع ظلال ناعمة
├── border-radius للبطاقات والعناصر الداخلية
├── عنوان ترحيبي: "مرحباً بك في دراسة الجدوى الخاصة بك"
├── نص تعريفي احترافي عن المستند (قابل للتحرير)
├── ملخص سريع لما سيتم عرضه (نقاط مرقمة)
└── أيقونات صغيرة توضيحية لكل قسم

النوع: نصف ثابت (العناوين ثابتة، النصوص متغيرة)

تفاصيل التصميم:
├── بطاقة بيضاء مركزية مع ظل
├── padding واسع ومريح
├── خط Dubai للعربية
├── أيقونات Lucide ملونة
└── تأثير fade-in عند العرض
```

### القسم 4.3: شريحة الغرف والمكونات (RoomsSlide) 🏠

**ملف: `components/feasibility/slides/RoomsSlide.tsx`**

> **الهدف**: تحديد ما تنقصه شقة العميل من تجهيزات ومصايف، مع حساب التكلفة الكاملة

```
الهيكل:
├── 1️⃣ اختيار عدد الغرف أولاً:
│   ├── Dropdown أو Input لتحديد العدد
│   ├── ترقيم تلقائي للغرف (غرفة 1، غرفة 2، ...)
│   └── إمكانية تحديد نوع كل غرفة
│
├── 2️⃣ لكل غرفة/قسم:
│   ├── رقم الغرفة والنوع (غرفة نوم 1، مطبخ، صالة، إلخ)
│   ├── ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│   ├── 📦 منطقة السحب والإفلات للعناصر:
│   │   ├── شبكة Grid منظمة
│   │   ├── عناصر تُسحب من القائمة الجانبية
│   │   ├── Snap to Grid عند الإفلات
│   │   └── لا يمكن إخراج العنصر من الغرفة
│   ├── ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│   ├── 🖼️ مربع صورة اختياري (يمين أو يسار):
│   │   ├── border-radius + ظل
│   │   ├── رفع صورة للغرفة
│   │   └── تصميم متطابق مع الملف
│   ├── ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│   ├── 📋 قائمة العناصر المضافة:
│   │   ├── اسم العنصر + أيقونة
│   │   ├── الكمية (قابلة للتعديل)
│   │   └── السعر × الكمية
│   └── 💰 إجمالي تكلفة الغرفة (يظهر أسفل كل غرفة)
│
└── 3️⃣ الإجمالي النهائي:
    └── تكلفة الشقة كاملة (مجموع كل الغرف)

📦 القائمة الجانبية للعناصر:
├── تبويبات حسب النوع:
│   ├── 🍳 المطبخ: مايكرويف، غلاية، خلاط، ثلاجة، فرن، بوتاجاز...
│   ├── 🛏️ غرفة النوم: سرير، دولاب، تسريحة، مكيف، كومودينو...
│   ├── 🚿 الحمام: غسالة، سخان، مرآة، فوط...
│   └── 🛋️ الصالة: كنبة، تلفزيون، طاولة، ستائر، سجادة...
├── كل عنصر داخل مربع واضح
├── أيقونة جرافيك ملونة
├── اسم العنصر
└── السعر الافتراضي

التصميم:
├── خلفية بيج (#f5ebe0) مع ظلال
├── كل غرفة في بطاقة منفصلة (border-radius)
├── الصور بتصميم موحد (border-radius + ظل)
├── العناصر في شبكة 3-4 أعمدة (Responsive)
├── الأسعار بتنسيق العملة المصرية
└── تأثيرات hover على العناصر

النوع: ديناميكي بالكامل (سحب وإفلات مع قيود)

مكونات فرعية:
├── RoomCard.tsx - بطاقة الغرفة الواحدة
├── ItemDropZone.tsx - منطقة الإفلات مع Snap to Grid
├── DraggableItem.tsx - العنصر القابل للسحب
├── ItemPreview.tsx - معاينة العنصر داخل الغرفة
├── RoomImageUploader.tsx - رفع صورة الغرفة
├── QuantitySelector.tsx - تحديد الكمية
└── CostCalculator.tsx - حساب التكلفة التلقائي
```

### القسم 4.4: شريحة الخريطة ودراسة المنطقة (MapSlide) 🗺️

**ملف: `components/feasibility/slides/MapSlide.tsx`**

> **الهدف**: دراسة جدوى للمناطق المحيطة - عرض الشقق المجاورة وأسعارها ومعلوماتها

**المتطلبات:**

```bash
npm install @react-google-maps/api
# أو (مجاني وأسهل)
npm install react-leaflet leaflet @types/leaflet
```

```
الهيكل:
├── 1️⃣ مقدمة تعريفية:
│   ├── عنوان "دراسة المنطقة المحيطة"
│   ├── نص تعريفي عما سيتم عرضه (قابل للتحرير)
│   └── تصميم وتنسيق جميل مع أيقونات
│
├── 2️⃣ الخريطة التفاعلية:
│   ├── ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│   ├── مستطيل كبير بـ border-radius + ظل
│   ├── خريطة Google Maps أو Leaflet
│   ├── ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│   ├── 📍 دبوس رئيسي: موقع شقة العميل (لون مميز)
│   ├── 📍 10 دبابيس كحد أقصى: الشقق المحيطة المعروضة للإيجار
│   │   ├── يتم وضعها يدوياً بالنقر على الخريطة
│   │   ├── أو إدخال الإحداثيات
│   │   └── لون مختلف عن الدبوس الرئيسي
│   └── ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│
├── 3️⃣ النافذة المنبثقة (عند النقر على دبوس):
│   ├── مستطيل عائم صغير بتصميم جميل
│   ├── 💰 سعر الإيجار
│   ├── 🛏️ عدد الغرف (أيقونة + رقم)
│   ├── ✨ المزايا باختصار
│   ├── 📏 المساحة
│   └── ملاحظات إضافية (نص يدوي)
│
└── 4️⃣ جدول الشقق المحيطة (أسفل الخريطة):
    ├── ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ├── جدول أنيق بـ 10 صفوف كحد أقصى
    ├── ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ├── الأعمدة:
    │   ├── رقم الشقة (#1-10)
    │   ├── السعر الشهري
    │   ├── عدد الغرف
    │   ├── 🔄 كم مرة تم تأجيرها
    │   ├── 📈 أعلى سعر إيجار تم بيعه
    │   └── ملاحظات مختصرة
    ├── ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    ├── تصميم بأيقونات صغيرة ملونة
    ├── صفوف متناوبة الألوان
    └── Responsive للشاشات الصغيرة

التصميم:
├── خلفية بيج مع بطاقات بيضاء
├── الخريطة داخل إطار مع border-radius + ظل
├── الدبابيس بألوان الهوية
├── النوافذ المنبثقة أنيقة وصغيرة
├── الجدول بتصميم حديث
└── أيقونات Lucide لكل معلومة

النوع: ديناميكي + متغير
├── الدبابيس: ديناميكية (يمكن إضافتها/حذفها)
├── المعلومات: متغيرة (قابلة للتحرير)
└── المقدمة: متغيرة

مكونات فرعية:
├── MapContainer.tsx - حاوية الخريطة مع الإعدادات
├── PropertyPin.tsx - دبوس العقار (قابل للسحب)
├── PropertyPopup.tsx - النافذة المنبثقة عند النقر
├── PinEditor.tsx - محرر معلومات الدبوس
└── NearbyPropertiesTable.tsx - جدول الشقق المحيطة
```

### القسم 4.5: شريحة الملخص والإحصائيات (SummarySlide) 📊

**ملف: `components/feasibility/slides/SummarySlide.tsx`**

> **الهدف**: خاتمة وملخص وإحصائيات ورسوم بيانية توضح كل ما تم التحدث عنه بشكل مبسط

```
الهيكل:
├── 1️⃣ عنوان "ملخص الدراسة":
│   ├── تصميم أنيق
│   └── فاصل زخرفي
│
├── 2️⃣ الرسوم البيانية التوضيحية:
│   ├── ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│   ├── 🥧 Pie Chart: توزيع التكاليف حسب الغرف
│   │   └── (المطبخ 30%، غرفة 1: 25%، الصالة 20%، ...)
│   ├── ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│   ├── 📊 Bar Chart: مقارنة سعر شقة العميل مع الشقق المحيطة
│   │   └── (شقتك vs الشقة 1 vs الشقة 2 vs ...)
│   ├── ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
│   └── 📈 Line Chart (اختياري): تطور أسعار الإيجار في المنطقة
│
├── 3️⃣ جدول ملخص التكاليف:
│   ├── كل غرفة وتكلفتها
│   ├── التكلفة الإجمالية للتجهيزات
│   ├── متوسط الإيجار المتوقع
│   └── العائد الاستثماري المقدر
│
├── 4️⃣ ملخص الشقق المحيطة:
│   ├── متوسط الأسعار في المنطقة
│   ├── أعلى وأقل سعر
│   ├── إجمالي مرات التأجير
│   └── نسبة الإشغال
│
├── 5️⃣ التوصيات:
│   ├── نص قابل للتحرير
│   ├── نقاط إيجابية ✅
│   ├── نقاط تحتاج اهتمام ⚠️
│   └── اقتراحات تحسين 💡
│
└── 6️⃣ الإجمالي النهائي:
    ├── رقم كبير واضح
    ├── تفصيل مختصر
    └── تأثير highlight

التصميم:
├── خلفية بيج مع بطاقات بيضاء
├── الرسوم البيانية بألوان الهوية
├── ظلال ناعمة
├── border-radius لكل بطاقة
├── أرقام كبيرة واضحة
└── أيقونات ملونة

النوع: ديناميكي (يُحسب تلقائياً من البيانات المدخلة)

مكتبة الرسوم البيانية:
npm install recharts
```

### القسم 4.6: شريحة الخاتمة (FooterSlide) 📞

**ملف: `components/feasibility/slides/FooterSlide.tsx`**

```
التصميم:
├── 1️⃣ نص ختامي:
│   ├── "شكراً لثقتكم بمفتاحك"
│   ├── نص تحفيزي (قابل للتحرير)
│   └── دعوة للتواصل
│
├── 2️⃣ الشعار:
│   ├── شعار مفتاحك بحجم كبير
│   └── الشعار النصي
│
├── 3️⃣ وسائل التواصل (أيقونات ملونة):
│   ├── 📱 واتساب (مع الرقم)
│   ├── 📸 إنستجرام
│   ├── 📘 فيسبوك
│   ├── ✉️ البريد الإلكتروني
│   └── 🌐 الموقع الإلكتروني
│
├── 4️⃣ معلومات الاتصال:
│   ├── رقم الهاتف
│   ├── العنوان
│   └── ساعات العمل
│
└── 5️⃣ توقيع:
    └── "تم إنشاء هذه الدراسة بواسطة منصة مفتاحك"

التصميم:
├── خلفية أخضر داكن (#10302b) أو بيج
├── نص بألوان فاتحة/ذهبية
├── أيقونات دائرية للسوشيال ميديا
├── تأثير hover على الأيقونات
└── محاذاة مركزية

النوع: ثابت بالكامل (لا يتغير بين الدراسات)
```

---

## 🔷 المرحلة الخامسة: نظام المشاركة والإرسال

> **سير العمل**: الأدمن ينهي الدراسة → ينقر "إرسال" → النظام ينشئ رابط خاص → يُرسل للعميل عبر الإيميل + يمكن نسخه ومشاركته

### الخطوة 5.1: API إنشاء رابط المشاركة

**ملف: `app/api/feasibility/[id]/share/route.ts`**

```typescript
// POST: إنشاء رابط مشاركة فريد
// المتطلبات:
// 
// 1️⃣ إنشاء shareId فريد (cuid أو uuid)
// 2️⃣ بناء URL المشاركة: https://moftahak.com/study/[shareId]
// 3️⃣ حفظ الرابط في قاعدة البيانات
// 4️⃣ إرسال بريد إلكتروني للعميل يحتوي على:
//    ├── رابط الدراسة
//    ├── اسم العميل
//    ├── ملخص سريع
//    └── تصميم بريد احترافي بألوان الهوية
// 5️⃣ تحديث حالة الدراسة إلى "SENT"
// 6️⃣ تسجيل تاريخ الإرسال (sentAt)
//
// Response:
// {
//   shareUrl: "https://moftahak.com/study/abc123",
//   emailSent: true,
//   sentAt: "2026-01-06T..."
// }
```

### الخطوة 5.2: صفحة عرض الدراسة للعميل

**ملف: `app/study/[shareId]/page.tsx`**

```
المتطلبات:
- صفحة عامة (لا تتطلب تسجيل دخول)
- عرض الدراسة بشكل كامل (للقراءة فقط)
- تصميم متجاوب (موبايل، تابلت، ديسكتوب)
- زر تحميل PDF (اختياري)
- تحديث حالة الدراسة إلى "VIEWED" عند الفتح
```

### الخطوة 5.3: إرسال البريد الإلكتروني

**المتطلبات:**

```bash
npm install nodemailer
# أو استخدام خدمة مثل SendGrid/Resend
npm install @sendgrid/mail
# أو
npm install resend
```

**ملف: `lib/email.ts`**

```typescript
// إرسال بريد يحتوي على:
// - رابط الدراسة
// - اسم العميل
// - ملخص سريع
// - تصميم بريد احترافي بألوان الهوية
```

### الخطوة 5.4: نظام نسخ الرابط

```
المتطلبات:
- زر نسخ الرابط
- إشعار "تم النسخ" باستخدام toast
- أيقونة مشاركة واتساب مباشرة
```

---

## 🔷 المرحلة السادسة: التحسينات والتجميل

### الخطوة 6.1: التموضع الذكي (Smart Positioning) - Snap to Grid

**ملف: `lib/feasibility/positioning.ts`**

> **قاعدة ذهبية**: العناصر يمكن سحبها بحرية لكن عند الإفلات تتحرك تلقائياً لأقرب مكان صحيح

```typescript
// نظام الـ Snap to Grid
// المتطلبات:
// 
// 1️⃣ تحديد شبكة Grid لكل منطقة (قسم):
//    ├── المطبخ: grid 4x4
//    ├── غرفة النوم: grid 3x4
//    ├── الصالة: grid 4x3
//    └── الحمام: grid 2x3
//
// 2️⃣ قواعد التموضع:
//    ├── العنصر يبقى داخل قسمه فقط (لا يخرج منه)
//    ├── لا يمكن نقل عنصر من قسم لآخر
//    ├── عند الإفلات في مكان غير دقيق → يتحرك لأقرب خلية فارغة
//    ├── عند الإفلات فوق عنصر آخر → يتبادلان الأماكن أو يرفض
//    └── Animation سلس عند التموضع التلقائي
//
// 3️⃣ منع التداخل:
//    ├── كل خلية تحتوي عنصر واحد فقط
//    ├── حساب أقرب خلية فارغة
//    └── إظهار مؤشر للمكان المتاح عند السحب
//
// 4️⃣ Responsive:
//    ├── الشبكة تتكيف مع حجم الشاشة
//    ├── تقليل الأعمدة على الموبايل
//    └── الحفاظ على النسب والتباعد

// مثال الكود:
interface GridConfig {
  columns: number;
  rows: number;
  cellWidth: number;
  cellHeight: number;
  gap: number;
}

function snapToGrid(position: Position, gridConfig: GridConfig): Position {
  const snappedX = Math.round(position.x / gridConfig.cellWidth) * gridConfig.cellWidth;
  const snappedY = Math.round(position.y / gridConfig.cellHeight) * gridConfig.cellHeight;
  return { x: snappedX, y: snappedY, zone: position.zone };
}

function isValidPosition(position: Position, zone: string, occupiedCells: Position[]): boolean {
  // التحقق من أن الموقع داخل القسم الصحيح
  // والتحقق من عدم وجود عنصر آخر في نفس المكان
}
```

### الخطوة 6.2: نظام Undo/Redo

**ملف: `hooks/useUndoRedo.ts`**

```typescript
// تتبع التغييرات
// - Ctrl+Z للتراجع
// - Ctrl+Y للإعادة
// - حد أقصى 50 خطوة
```

### الخطوة 6.3: الحفظ التلقائي

```typescript
// حفظ كل 30 ثانية
// حفظ عند أي تغيير مهم
// مؤشر حالة الحفظ (محفوظ، جاري الحفظ)
```

### الخطوة 6.4: المعاينة

**ملف: `components/feasibility/viewer/PreviewModal.tsx`**

```
المتطلبات:
- معاينة كاملة للدراسة
- التنقل بين الشرائح
- عرض كما سيراها العميل
- زر إغلاق
```

### الخطوة 6.5: تصدير PDF (اختياري)

```bash
npm install @react-pdf/renderer
# أو
npm install html2canvas jspdf
```

---

## 📦 بيانات العناصر الافتراضية

**ملف: `lib/feasibility/items-data.ts`**

```typescript
export const kitchenItems = [
  { id: 'microwave', nameAr: 'مايكرويف', nameEn: 'Microwave', icon: 'Microwave', price: 2500 },
  { id: 'kettle', nameAr: 'غلاية', nameEn: 'Kettle', icon: 'Coffee', price: 300 },
  { id: 'blender', nameAr: 'خلاط', nameEn: 'Blender', icon: 'Blend', price: 500 },
  { id: 'refrigerator', nameAr: 'ثلاجة', nameEn: 'Refrigerator', icon: 'Refrigerator', price: 8000 },
  { id: 'oven', nameAr: 'فرن', nameEn: 'Oven', icon: 'Oven', price: 3500 },
  { id: 'toaster', nameAr: 'توستر', nameEn: 'Toaster', icon: 'Sandwich', price: 400 },
  { id: 'dishes', nameAr: 'أطباق وأدوات', nameEn: 'Dishes', icon: 'UtensilsCrossed', price: 800 },
  { id: 'gas_stove', nameAr: 'بوتاجاز', nameEn: 'Gas Stove', icon: 'Flame', price: 5000 },
  // ... المزيد
];

export const bedroomItems = [
  { id: 'bed_single', nameAr: 'سرير مفرد', nameEn: 'Single Bed', icon: 'Bed', price: 3000 },
  { id: 'bed_double', nameAr: 'سرير مزدوج', nameEn: 'Double Bed', icon: 'BedDouble', price: 5000 },
  { id: 'wardrobe', nameAr: 'دولاب', nameEn: 'Wardrobe', icon: 'Door', price: 4000 },
  { id: 'dresser', nameAr: 'تسريحة', nameEn: 'Dresser', icon: 'Square', price: 2000 },
  { id: 'ac', nameAr: 'مكيف', nameEn: 'Air Conditioner', icon: 'Wind', price: 12000 },
  { id: 'nightstand', nameAr: 'كومودينو', nameEn: 'Nightstand', icon: 'BoxSelect', price: 800 },
  // ... المزيد
];

export const bathroomItems = [
  { id: 'washing_machine', nameAr: 'غسالة', nameEn: 'Washing Machine', icon: 'Drum', price: 7000 },
  { id: 'water_heater', nameAr: 'سخان', nameEn: 'Water Heater', icon: 'Thermometer', price: 2500 },
  { id: 'mirror', nameAr: 'مرآة', nameEn: 'Mirror', icon: 'Square', price: 500 },
  { id: 'towels', nameAr: 'فوط', nameEn: 'Towels', icon: 'Shirt', price: 300 },
  // ... المزيد
];

export const livingRoomItems = [
  { id: 'sofa', nameAr: 'كنبة', nameEn: 'Sofa', icon: 'Sofa', price: 8000 },
  { id: 'tv', nameAr: 'تلفزيون', nameEn: 'TV', icon: 'Tv', price: 6000 },
  { id: 'coffee_table', nameAr: 'طاولة قهوة', nameEn: 'Coffee Table', icon: 'Table', price: 1500 },
  { id: 'curtains', nameAr: 'ستائر', nameEn: 'Curtains', icon: 'Blinds', price: 2000 },
  { id: 'carpet', nameAr: 'سجادة', nameEn: 'Carpet', icon: 'Square', price: 1500 },
  { id: 'dining_table', nameAr: 'طاولة طعام', nameEn: 'Dining Table', icon: 'Table2', price: 3000 },
  // ... المزيد
];
```

---

## 🎨 ملاحظات التصميم

### الألوان المستخدمة

```css
/* من هوية الموقع */
--primary: #edbf8c;      /* ذهبي فاتح */
--secondary: #10302b;    /* أخضر داكن */
--accent: #ead3b9;       /* بيج فاتح */
--white: #fdf6ee;        /* أبيض مصفر */

/* للدراسة */
--cover-bg: #10302b;     /* غلاف أخضر */
--slide-bg: #f5ebe0;     /* خلفية شرائح بيج */
--card-bg: #fdf6ee;      /* خلفية البطاقات */
--shadow: rgba(16, 48, 43, 0.1);
```

### قواعد التصميم

1. **الغلاف فقط**: خلفية خضراء (#10302b)
2. **باقي الشرائح**: درجات البيج مع ظلال
3. **جميع العناصر**: border-radius (مخالف لسياسة الصفحة الرئيسية - خاص بالدراسة)
4. **الصور**: border-radius موحد + ظل
5. **الخطوط**: Dubai للعربية، Bristone للإنجليزية

---

## ✅ قائمة المهام (Checklist)

### المرحلة الأولى
- [ ] تحديث schema.prisma
- [ ] تنفيذ migration
- [ ] إنشاء ملف الأنواع feasibility.ts
- [ ] إنشاء هيكل المجلدات

### المرحلة الثانية
- [ ] API طلبات الاستشارات
- [ ] صفحة قائمة الطلبات
- [ ] مكون بطاقة الطلب

### المرحلة الثالثة
- [ ] تثبيت @dnd-kit
- [ ] مكون المحرر الرئيسي
- [ ] الشريط الجانبي للعناصر
- [ ] منطقة العمل
- [ ] مكون النص القابل للتحرير

### المرحلة الرابعة
- [ ] شريحة الغلاف
- [ ] شريحة المقدمة
- [ ] شريحة الغرف والمكونات
- [ ] شريحة الخريطة
- [ ] شريحة الملخص
- [ ] شريحة الخاتمة

### المرحلة الخامسة
- [ ] API رابط المشاركة
- [ ] صفحة عرض الدراسة
- [ ] إرسال البريد الإلكتروني
- [ ] نظام نسخ الرابط

### المرحلة السادسة
- [ ] التموضع الذكي
- [ ] نظام Undo/Redo
- [ ] الحفظ التلقائي
- [ ] المعاينة
- [ ] تصدير PDF (اختياري)

---

## 📝 ملاحظات تنفيذية

1. **الأولوية للـ MVP**: ابدأ بالميزات الأساسية ثم أضف التحسينات
2. **الاختبار**: اختبر السحب والإفلات على أجهزة متعددة
3. **الأداء**: استخدم React.memo و useMemo للعناصر المتكررة
4. **التوافقية**: الخريطة تحتاج API key من Google Maps
5. **البريد**: يحتاج إعداد SMTP أو خدمة بريد

---

## 🚀 أوامر البدء

```bash
# 1. تثبيت الحزم المطلوبة
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities recharts resend

# 2. للخريطة (اختر واحدة)
npm install @react-google-maps/api
# أو
npm install react-leaflet leaflet @types/leaflet

# 3. تحديث قاعدة البيانات
npx prisma migrate dev --name add_feasibility_system
npx prisma generate

# 4. إنشاء المتغيرات البيئية
# أضف في .env:
# GOOGLE_MAPS_API_KEY=your_key
# RESEND_API_KEY=your_key
```

---

## 🔒 الأمان والخصوصية

### 8.1: أمان الروابط المشتركة

```typescript
// shareId Security Requirements:
// ├── استخدام UUID v4 أو token طويل (32+ حرف)
// ├── غير قابل للتخمين (cryptographically secure)
// ├── إضافة expiresAt - صلاحية زمنية للرابط
// ├── خيار كلمة مرور للرابط (password protection)
// ├── إمكانية تعطيل الرابط يدوياً
// └── تسجيل المشاهدات (views count + IP + timestamp)

// إضافة للـ Schema:
model FeasibilityStudy {
  // ... الحقول الموجودة
  expiresAt     DateTime?  // تاريخ انتهاء صلاحية الرابط
  password      String?    // كلمة مرور مشفرة (اختياري)
  isDisabled    Boolean    @default(false)  // تعطيل الرابط
  viewCount     Int        @default(0)
}

model StudyView {
  id        String   @id @default(cuid())
  studyId   String
  study     FeasibilityStudy @relation(fields: [studyId], references: [id])
  ip        String?
  userAgent String?
  viewedAt  DateTime @default(now())
}
```

### 8.2: التحقق من المدخلات (Server-side Validation)

```typescript
// lib/validations/feasibility.ts
import { z } from 'zod';

export const studyContentSchema = z.object({
  slides: z.array(z.object({
    id: z.string(),
    type: z.enum(['cover', 'intro', 'rooms', 'map', 'summary', 'footer']),
    order: z.number().min(0).max(10),
    content: z.object({}).passthrough()
  })).max(20),  // حد أقصى للشرائح
  rooms: z.array(z.object({
    id: z.string(),
    type: z.enum(['bedroom', 'bathroom', 'kitchen', 'living_room']),
    items: z.array(z.any()).max(50)  // حد أقصى للعناصر
  })).max(10),  // حد أقصى للغرف
  // ... validation للباقي
});

// التحقق قبل الحفظ في كل API
```

### 8.3: حماية API

```typescript
// Rate Limiting
// ├── POST /api/feasibility: 10 requests/minute
// ├── POST /api/share: 5 requests/minute  
// ├── GET /api/study/[shareId]: 30 requests/minute
// └── File upload: 5 MB max, image types only

// CSRF Protection
// ├── استخدام next-csrf أو tokens
// └── التحقق من origin headers

// File Upload Security
// ├── التحقق من MIME type (not just extension)
// ├── حد أقصى للحجم: 5MB للصور
// ├── مسح الملفات (virus scanning) إن أمكن
// └── تخزين خارجي (S3) وليس في DB
```

### 8.4: حماية بيانات العملاء (PII)

```typescript
// البيانات الحساسة:
// ├── clientEmail → تشفير في DB
// ├── clientName → لا تضعه في URL
// ├── location/coordinates → التعامل بحذر
// └── معلومات الشقق → audit trail

// Audit Trail
model AuditLog {
  id          String   @id @default(cuid())
  userId      String
  action      String   // CREATE, UPDATE, DELETE, VIEW, SHARE
  entityType  String   // FeasibilityStudy, ConsultationRequest
  entityId    String
  changes     Json?    // التغييرات (before/after)
  ip          String?
  createdAt   DateTime @default(now())
}
```

---

## 📊 تحسينات الأداء والتوسع

### 9.1: إعادة هيكلة قاعدة البيانات (Hybrid Approach)

> **المشكلة**: حقل `content: Json` كبير يسبب مشاكل أداء عند التحديث المتكرر

```prisma
// الحل: هيكل هجين - metadata منفصل عن المحتوى

model FeasibilityStudy {
  id            String   @id @default(cuid())
  title         String
  clientName    String
  clientEmail   String
  shareId       String   @unique @default(cuid())
  totalCost     Float    @default(0)
  status        StudyStatus @default(DRAFT)
  
  // العلاقات بدلاً من JSON
  slides        Slide[]
  rooms         StudyRoom[]
  nearbyProperties NearbyProperty[]
  settings      Json?    // إعدادات صغيرة فقط
  
  // للـ snapshot/export فقط
  contentSnapshot Json?  // نسخة كاملة عند الإرسال
  
  createdBy     String
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  
  @@index([shareId])
  @@index([createdBy])
  @@index([createdAt])
  @@index([status])
}

model Slide {
  id        String   @id @default(cuid())
  studyId   String
  study     FeasibilityStudy @relation(fields: [studyId], references: [id], onDelete: Cascade)
  type      SlideType
  order     Int
  title     String?
  subtitle  String?
  description String? @db.Text
  customData Json?
  
  @@index([studyId, order])
}

model StudyRoom {
  id        String   @id @default(cuid())
  studyId   String
  study     FeasibilityStudy @relation(fields: [studyId], references: [id], onDelete: Cascade)
  type      RoomType
  name      String
  number    Int
  imageUrl  String?
  totalCost Float    @default(0)
  items     StudyRoomItem[]
  
  @@index([studyId])
}

model StudyRoomItem {
  id        String   @id @default(cuid())
  roomId    String
  room      StudyRoom @relation(fields: [roomId], references: [id], onDelete: Cascade)
  itemId    String   // reference to FeasibilityItem
  name      String
  icon      String
  price     Float
  quantity  Int      @default(1)
  positionX Float    @default(0)
  positionY Float    @default(0)
  
  @@index([roomId])
}
```

### 9.2: تخزين الصور (Object Storage)

```typescript
// lib/storage.ts
// ❌ لا تخزن الصور في DB
// ✅ استخدم S3 أو Cloudflare R2

import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3';
import { getSignedUrl } from '@aws-sdk/s3-request-presigner';

export async function uploadImage(file: File, studyId: string): Promise<string> {
  // 1. التحقق من نوع الملف
  const allowedTypes = ['image/jpeg', 'image/png', 'image/webp'];
  if (!allowedTypes.includes(file.type)) {
    throw new Error('نوع ملف غير مسموح');
  }
  
  // 2. التحقق من الحجم (5MB max)
  if (file.size > 5 * 1024 * 1024) {
    throw new Error('حجم الملف كبير جداً');
  }
  
  // 3. رفع إلى S3
  const key = `studies/${studyId}/${Date.now()}-${file.name}`;
  // ... upload logic
  
  // 4. إرجاع URL
  return `https://cdn.moftahak.com/${key}`;
}

// استخدام Pre-signed URLs للرفع المباشر من المتصفح
export async function getUploadUrl(studyId: string, filename: string) {
  // ... generate pre-signed URL
}
```

### 9.3: Lazy Loading للمحتوى

```typescript
// app/study/[shareId]/page.tsx
// تحميل تدريجي للشرائح

// API: GET /api/study/[shareId]
// يُرجع: metadata + أول شريحتين فقط

// API: GET /api/study/[shareId]/slide/[order]  
// يُرجع: شريحة واحدة عند الطلب

// في الواجهة:
const StudyViewer = () => {
  const [visibleSlides, setVisibleSlides] = useState([0, 1]);
  
  // تحميل الشريحة التالية عند الاقتراب منها
  const loadNextSlide = useCallback((slideIndex: number) => {
    // Intersection Observer
  }, []);
};
```

### 9.4: أداء Drag & Drop

```typescript
// للعناصر الكثيرة - استخدم Virtualization
import { FixedSizeGrid } from 'react-window';

// في EditorSidebar - قائمة العناصر
const ItemsGrid = ({ items, category }) => {
  return (
    <FixedSizeGrid
      columnCount={3}
      rowCount={Math.ceil(items.length / 3)}
      columnWidth={100}
      rowHeight={100}
      width={320}
      height={400}
    >
      {({ columnIndex, rowIndex, style }) => {
        const item = items[rowIndex * 3 + columnIndex];
        return item ? <DraggableItem item={item} style={style} /> : null;
      }}
    </FixedSizeGrid>
  );
};

// تحسينات إضافية:
// ├── React.memo للعناصر
// ├── useMemo للحسابات
// ├── useCallback للـ handlers
// └── اختبار على أجهزة موبايل حقيقية
```

---

## 🔧 State Management (Zustand + Immer)

### 10.1: إعداد Store للمحرر

```typescript
// lib/feasibility/store.ts
import { create } from 'zustand';
import { immer } from 'zustand/middleware/immer';
import { temporal } from 'zundo';  // للـ undo/redo

interface EditorState {
  study: FeasibilityStudy | null;
  selectedElement: string | null;
  isDirty: boolean;
  isSaving: boolean;
  history: HistoryState;
  
  // Actions
  setStudy: (study: FeasibilityStudy) => void;
  updateSlide: (slideId: string, updates: Partial<Slide>) => void;
  addItemToRoom: (roomId: string, item: RoomItem) => void;
  moveItem: (roomId: string, itemId: string, position: Position) => void;
  removeItem: (roomId: string, itemId: string) => void;
  updateItemQuantity: (roomId: string, itemId: string, quantity: number) => void;
  
  // Undo/Redo
  undo: () => void;
  redo: () => void;
  canUndo: boolean;
  canRedo: boolean;
}

export const useEditorStore = create<EditorState>()(
  temporal(
    immer((set, get) => ({
      study: null,
      selectedElement: null,
      isDirty: false,
      isSaving: false,
      
      setStudy: (study) => set({ study, isDirty: false }),
      
      updateSlide: (slideId, updates) => set((state) => {
        const slide = state.study?.slides.find(s => s.id === slideId);
        if (slide) {
          Object.assign(slide, updates);
          state.isDirty = true;
        }
      }),
      
      addItemToRoom: (roomId, item) => set((state) => {
        const room = state.study?.rooms.find(r => r.id === roomId);
        if (room) {
          room.items.push(item);
          room.totalCost += item.price * item.quantity;
          state.study!.totalCost += item.price * item.quantity;
          state.isDirty = true;
        }
      }),
      
      moveItem: (roomId, itemId, newPosition) => set((state) => {
        const room = state.study?.rooms.find(r => r.id === roomId);
        const item = room?.items.find(i => i.id === itemId);
        if (item) {
          // Snap to grid
          item.position = snapToGrid(newPosition, getGridConfig(room.type));
          state.isDirty = true;
        }
      }),
      
      // ... المزيد من الـ actions
    })),
    {
      limit: 50,  // حد أقصى 50 خطوة للـ history
      equality: (a, b) => JSON.stringify(a) === JSON.stringify(b),
    }
  )
);
```

### 10.2: الحفظ التلقائي (Auto-save)

```typescript
// hooks/useAutoSave.ts
import { useEffect, useRef } from 'react';
import { useEditorStore } from '@/lib/feasibility/store';
import { debounce } from 'lodash';

export function useAutoSave(studyId: string) {
  const { study, isDirty, isSaving } = useEditorStore();
  const lastSaved = useRef<Date>(new Date());
  
  // حفظ عند التغيير (debounced)
  const debouncedSave = useMemo(
    () => debounce(async () => {
      if (!isDirty || isSaving) return;
      
      useEditorStore.setState({ isSaving: true });
      try {
        await fetch(`/api/feasibility/${studyId}`, {
          method: 'PATCH',
          body: JSON.stringify(study),
        });
        useEditorStore.setState({ isDirty: false });
        lastSaved.current = new Date();
      } catch (error) {
        console.error('فشل الحفظ التلقائي:', error);
        // toast.error('فشل الحفظ');
      } finally {
        useEditorStore.setState({ isSaving: false });
      }
    }, 2000),  // انتظر 2 ثانية بعد آخر تغيير
    [study, isDirty, studyId]
  );
  
  useEffect(() => {
    if (isDirty) {
      debouncedSave();
    }
  }, [isDirty, debouncedSave]);
  
  // حفظ دوري كل 30 ثانية
  useEffect(() => {
    const interval = setInterval(() => {
      if (isDirty && !isSaving) {
        debouncedSave.flush();
      }
    }, 30000);
    return () => clearInterval(interval);
  }, [isDirty, isSaving]);
  
  // مؤشر الحالة
  return {
    isSaving,
    isDirty,
    lastSaved: lastSaved.current,
    saveNow: () => debouncedSave.flush(),
  };
}
```

---

## 📄 توليد PDF (Server-side)

### 11.1: استخدام Puppeteer للجودة العالية

```typescript
// app/api/feasibility/[id]/pdf/route.ts
import puppeteer from 'puppeteer';

export async function GET(
  request: Request,
  { params }: { params: { id: string } }
) {
  const study = await prisma.feasibilityStudy.findUnique({
    where: { id: params.id },
    include: { slides: true, rooms: { include: { items: true } } }
  });
  
  if (!study) {
    return new Response('Not found', { status: 404 });
  }
  
  // تشغيل Puppeteer
  const browser = await puppeteer.launch({
    headless: 'new',
    args: ['--no-sandbox', '--disable-setuid-sandbox']
  });
  
  const page = await browser.newPage();
  
  // فتح صفحة العرض الخاصة
  await page.goto(`${process.env.APP_URL}/study/${study.shareId}/print`, {
    waitUntil: 'networkidle0'
  });
  
  // انتظار تحميل الخطوط والصور
  await page.evaluateHandle('document.fonts.ready');
  
  // توليد PDF
  const pdf = await page.pdf({
    format: 'A4',
    printBackground: true,
    margin: { top: '20mm', bottom: '20mm', left: '15mm', right: '15mm' },
    displayHeaderFooter: true,
    headerTemplate: '<div></div>',
    footerTemplate: `
      <div style="font-size: 10px; text-align: center; width: 100%;">
        صفحة <span class="pageNumber"></span> من <span class="totalPages"></span>
      </div>
    `
  });
  
  await browser.close();
  
  return new Response(pdf, {
    headers: {
      'Content-Type': 'application/pdf',
      'Content-Disposition': `attachment; filename="feasibility-study-${study.id}.pdf"`
    }
  });
}
```

### 11.2: ملاحظات للـ PDF

```
⚠️ تحذيرات:
├── Puppeteer يحتاج موارد (RAM/CPU) - لا تستخدمه في Serverless بدون تعديلات
├── استخدم @sparticuz/chromium-min للـ Vercel/Netlify
├── أو انقل توليد PDF لـ background worker (BullMQ)
└── بديل: استخدم خدمة PDF خارجية (DocRaptor, PDFShift)

✅ للـ MVP:
├── ابدأ بـ html2canvas + jspdf (client-side)
├── قد تفقد بعض الدقة لكنه يعمل
└── انتقل لـ Puppeteer لاحقاً للجودة العالية
```

---

## 🗺️ الخريطة - تحسينات

### 12.1: Leaflet (مجاني) vs Google Maps

```typescript
// الاختيار الموصى به للـ MVP: Leaflet
// ├── مجاني بالكامل
// ├── لا يحتاج API key
// ├── خرائط OpenStreetMap
// └── أخف وأسرع

// npm install react-leaflet leaflet @types/leaflet

// components/feasibility/slides/MapContainer.tsx
'use client';

import { MapContainer, TileLayer, Marker, Popup, useMapEvents } from 'react-leaflet';
import 'leaflet/dist/leaflet.css';

interface Props {
  center: [number, number];
  pins: NearbyProperty[];
  onAddPin: (lat: number, lng: number) => void;
  onPinClick: (pinId: string) => void;
  maxPins?: number;  // default 10
}

export function PropertyMap({ center, pins, onAddPin, onPinClick, maxPins = 10 }: Props) {
  return (
    <MapContainer 
      center={center} 
      zoom={15} 
      className="h-100 w-full rounded-xl shadow-lg"
    >
      <TileLayer
        url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"
        attribution='&copy; OpenStreetMap'
      />
      
      {/* دبوس العميل الرئيسي */}
      <Marker position={center} icon={mainPinIcon}>
        <Popup>شقة العميل</Popup>
      </Marker>
      
      {/* الشقق المحيطة */}
      {pins.map((pin) => (
        <Marker 
          key={pin.id} 
          position={[pin.coordinates.lat, pin.coordinates.lng]}
          icon={secondaryPinIcon}
          eventHandlers={{ click: () => onPinClick(pin.id) }}
        >
          <Popup>
            <PropertyPopup property={pin} />
          </Popup>
        </Marker>
      ))}
      
      {/* إضافة دبوس جديد بالنقر */}
      {pins.length < maxPins && (
        <AddPinHandler onAdd={onAddPin} />
      )}
    </MapContainer>
  );
}

// إدخال الإحداثيات يدوياً
export function ManualCoordinatesInput({ onSubmit }: { onSubmit: (lat: number, lng: number) => void }) {
  const [lat, setLat] = useState('');
  const [lng, setLng] = useState('');
  
  return (
    <div className="flex gap-2 p-4 bg-white rounded-lg shadow">
      <input
        type="number"
        placeholder="خط العرض (Latitude)"
        value={lat}
        onChange={(e) => setLat(e.target.value)}
        className="input"
        step="0.000001"
      />
      <input
        type="number"
        placeholder="خط الطول (Longitude)"
        value={lng}
        onChange={(e) => setLng(e.target.value)}
        className="input"
        step="0.000001"
      />
      <button 
        onClick={() => onSubmit(parseFloat(lat), parseFloat(lng))}
        className="btn-primary"
      >
        إضافة
      </button>
    </div>
  );
}
```

---

## ✅ MVP Checklist (أولوية التنفيذ)

### 🔴 المرحلة 1: الأساسيات (أسبوع 1-2)

```
□ 1.1 إعداد قاعدة البيانات
  ├── □ تحديث schema.prisma (الهيكل الهجين)
  ├── □ إضافة indexes
  ├── □ تنفيذ migration
  └── □ إنشاء seed data للاختبار

□ 1.2 API الأساسية
  ├── □ CRUD للـ ConsultationRequest
  ├── □ CRUD للـ FeasibilityStudy
  └── □ Validation middleware

□ 1.3 صفحة طلبات الاستشارات (Admin)
  ├── □ جدول الطلبات
  ├── □ فلترة بالحالة
  └── □ زر "إنشاء دراسة جدوى"
```

### 🟡 المرحلة 2: المحرر الأساسي (أسبوع 3-4)

```
□ 2.1 هيكل المحرر
  ├── □ Layout ثلاثي الأعمدة
  ├── □ شريط الأدوات العلوي
  └── □ Zustand store

□ 2.2 شريحة واحدة كـ Spike
  ├── □ RoomCard مع drag & drop أساسي
  ├── □ قائمة عناصر جانبية
  ├── □ Snap to grid بسيط
  └── □ حساب التكلفة

□ 2.3 تحرير النصوص
  └── □ EditableText component
```

### 🟢 المرحلة 3: المشاركة (أسبوع 5)

```
□ 3.1 إنشاء الرابط
  ├── □ API لإنشاء shareId آمن
  ├── □ صفحة عرض عامة (read-only)
  └── □ زر نسخ الرابط

□ 3.2 البريد الإلكتروني
  ├── □ إعداد Resend/SendGrid
  ├── □ قالب بريد بسيط
  └── □ إرسال عند النقر
```

### 🔵 MVP+ (بعد الإطلاق الأولي)

```
□ الشرائح الكاملة (Cover, Intro, Map, Summary, Footer)
□ Undo/Redo كامل
□ رفع الصور (S3)
□ الخريطة التفاعلية (Leaflet)
□ تصدير PDF
□ الرسوم البيانية (Recharts)
□ الحفظ التلقائي المتقدم
□ Audit trail
□ Mobile touch optimization
```

---

## 🧪 الاختبارات

### 13.1: Unit Tests

```typescript
// __tests__/lib/feasibility/pricing.test.ts
import { calculateRoomCost, calculateTotalCost } from '@/lib/feasibility/pricing';

describe('Pricing calculations', () => {
  it('should calculate room cost correctly', () => {
    const items = [
      { price: 1000, quantity: 2 },
      { price: 500, quantity: 1 },
    ];
    expect(calculateRoomCost(items)).toBe(2500);
  });
  
  it('should calculate total cost for all rooms', () => {
    const rooms = [
      { totalCost: 5000 },
      { totalCost: 3000 },
    ];
    expect(calculateTotalCost(rooms)).toBe(8000);
  });
});

// __tests__/lib/feasibility/positioning.test.ts
describe('Snap to Grid', () => {
  it('should snap position to nearest grid cell', () => {
    const position = { x: 55, y: 78, zone: 'kitchen' };
    const gridConfig = { cellWidth: 50, cellHeight: 50, columns: 4, rows: 4, gap: 10 };
    
    const snapped = snapToGrid(position, gridConfig);
    
    expect(snapped.x).toBe(50);
    expect(snapped.y).toBe(100);
  });
  
  it('should keep element within zone bounds', () => {
    // ...
  });
});
```

### 13.2: E2E Tests (Playwright)

```typescript
// e2e/feasibility-flow.spec.ts
import { test, expect } from '@playwright/test';

test.describe('Feasibility Study Flow', () => {
  test('Admin creates and shares a study', async ({ page }) => {
    // 1. تسجيل دخول كـ Admin
    await page.goto('/admin');
    await page.fill('[name="email"]', 'admin@moftahak.com');
    await page.fill('[name="password"]', 'password');
    await page.click('button[type="submit"]');
    
    // 2. الذهاب لطلبات الاستشارات
    await page.click('text=طلبات الاستشارات');
    await expect(page.locator('table')).toBeVisible();
    
    // 3. إنشاء دراسة جدوى
    await page.click('text=إنشاء دراسة جدوى');
    await expect(page.url()).toContain('/admin/feasibility/');
    
    // 4. إضافة عناصر
    await page.dragAndDrop('[data-item="microwave"]', '[data-room="kitchen"]');
    
    // 5. التحقق من حساب التكلفة
    await expect(page.locator('[data-total-cost]')).toHaveText(/2,500/);
    
    // 6. الإرسال
    await page.click('text=إرسال');
    await expect(page.locator('text=تم إرسال الرابط')).toBeVisible();
  });
  
  test('Client views shared study', async ({ page }) => {
    // فتح رابط مشترك
    await page.goto('/study/test-share-id');
    
    // التحقق من عرض المحتوى
    await expect(page.locator('text=دراسة جدوى عقارية')).toBeVisible();
    await expect(page.locator('[data-slide="cover"]')).toBeVisible();
  });
});
```

---

## 📝 الخطوات التالية (Action Items)

```
الآن (قبل البدء بالكود):
├── ✅ مراجعة هذا الملف والموافقة على الهيكل
├── □ اتخاذ قرار: JSON كامل vs جداول منفصلة (موصى: هجين)
├── □ إعداد حساب S3/R2 للصور
└── □ إعداد حساب Resend/SendGrid للبريد

Spike أولي (1-2 يوم):
├── □ إنشاء RoomCard بسيط مع drag & drop
├── □ اختبار الأداء على الموبايل
└── □ التحقق من أن @dnd-kit يعمل كما متوقع

بعد الـ Spike:
├── □ تنفيذ Migration نهائي
├── □ بناء API كاملة
└── □ بناء الواجهات
```

---

**تاريخ الإنشاء:** 6 يناير 2026  
**النسخة:** 1.1 (محدث بملاحظات المراجعة)  
**الحالة:** جاهز للتنفيذ بعد الموافقة على القرارات التصميمية

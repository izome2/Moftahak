# إعداد Vercel Blob Storage لرفع الصور

## ✅ ما تم إنجازه:

1. ✅ تثبيت `@vercel/blob`
2. ✅ تحديث API رفع الصور لاستخدام Vercel Blob
3. ✅ تحديث API حذف الصور مع حذف من Blob

---

## 🔧 خطوات الإعداد في Vercel Dashboard:

### 1. إنشاء Blob Store

1. اذهب إلى: https://vercel.com/dashboard
2. اختر مشروعك: **moftahak**
3. انقر على **Storage** من القائمة الجانبية
4. انقر على **Create Database**
5. اختر **Blob**
6. أعطها اسم: `moftahak-avatars` (أو أي اسم تفضله)
7. اختر Region: **Washington, D.C., USA (iad1)** - نفس region المشروع
8. انقر **Create**

### 2. ربط Blob Store بالمشروع

1. بعد إنشاء الـ Blob Store، سيطلب منك ربطه بالمشروع
2. اختر **Connect to Project**
3. اختر مشروع **moftahak**
4. اختر البيئات: **Production, Preview, Development**
5. انقر **Connect**

سيتم إضافة متغير البيئة `BLOB_READ_WRITE_TOKEN` تلقائياً!

---

## 🚀 إعادة النشر

بعد إعداد Blob Storage، قم بإعادة نشر المشروع:

```powershell
vercel --prod
```

---

## 🧪 اختبار رفع الصور

1. افتح الموقع: https://moftahak.vercel.app
2. سجّل دخول بحساب Admin أو أي مستخدم
3. انقر على صورة الملف الشخصي (أعلى يمين الصفحة)
4. اختر **تعديل الملف الشخصي**
5. اضغط على **تغيير الصورة**
6. اختر صورة وارفعها
7. ✅ يجب أن ترى الصورة تتحدث مباشرة!

---

## 📝 ملاحظات:

### حدود Vercel Blob (المجانية):
- **تخزين:** 100 GB
- **عرض النطاق:** 100 GB/شهر
- **عدد الطلبات:** 1 مليون طلب/شهر

### مزايا Vercel Blob:
- ✅ رفع وحذف تلقائي
- ✅ CDN عالمي لتحميل سريع
- ✅ لا حاجة لإعداد معقد
- ✅ آمن ومُدار بالكامل

### الصور المرفوعة:
- سيتم حفظها في مجلد `avatars/`
- الرابط سيكون مثل: `https://<blob-store>.public.blob.vercel-storage.com/avatars/user-id-timestamp.jpg`
- يمكن الوصول إليها من أي مكان (public access)

---

## 🔍 استكشاف الأخطاء

### خطأ: "BLOB_READ_WRITE_TOKEN is not defined"

**الحل:**
- تأكد من ربط Blob Store بالمشروع من Vercel Dashboard
- تحقق من وجود المتغير في: Settings → Environment Variables
- أعد النشر: `vercel --prod`

### خطأ: "Access denied"

**الحل:**
- تأكد من أن Access Mode للـ Blob Store هو **Public**
- يمكنك تغييره من: Storage → [Your Blob Store] → Settings

### الصورة لا تظهر:

**الحل:**
- افتح Developer Tools (F12)
- تحقق من Console للأخطاء
- تأكد من أن الرابط في قاعدة البيانات يبدأ بـ `https://`
- جرّب فتح رابط الصورة مباشرة في المتصفح

---

## 📊 إدارة Blob Storage

### عرض الصور المرفوعة:
1. Vercel Dashboard → Storage
2. اختر Blob Store الخاص بك
3. ستجد قائمة بكل الملفات المرفوعة

### حذف صور يدوياً:
1. في قائمة الملفات، انقر على الصورة
2. انقر **Delete**

### إحصائيات الاستخدام:
1. Storage → Usage
2. ستجد:
   - مساحة التخزين المستخدمة
   - عرض النطاق المستخدم
   - عدد الطلبات

---

## ✨ مميزات إضافية

### تحسين الصور تلقائياً:
Vercel Blob يدعم تحسين الصور عبر Next.js Image Optimization:

```tsx
import Image from 'next/image';

<Image 
  src={user.image} 
  width={100} 
  height={100} 
  alt="Avatar"
/>
```

### رفع ملفات أخرى:
يمكنك استخدام نفس النظام لرفع:
- صور دراسات الجدوى
- مستندات PDF
- ملفات أخرى

فقط غيّر المسار من `avatars/` إلى المسار المطلوب!

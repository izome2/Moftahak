# خطة تطوير نظام المصادقة والتسجيل - مفتاحك

## نظرة عامة
تطوير نظام مصادقة كامل مع واجهات مختلفة للمستخدمين العاديين والإداريين، مع الحفاظ على الهوية البصرية للموقع (ألوان البيج والذهبي والأخضر الداكن، بدون border-radius للعناصر الأساسية).

---

## المرحلة 1️⃣: تصميم واجهة تسجيل الدخول

### 1.1 إنشاء مكون Modal الرئيسي
**الملفات المطلوبة:**
- `components/auth/AuthModal.tsx` - المكون الرئيسي للـ modal
- `components/auth/LoginForm.tsx` - نموذج تسجيل الدخول
- `components/auth/RegisterForm.tsx` - نموذج التسجيل

**المواصفات التقنية:**
```typescript
// التصميم
- خلفية overlay: backdrop-blur-md bg-black/40
- المستطيل الأيمن (Login/Register):
  * لون: bg-accent (البيج الفاتح #ead3b9)
  * حواف: rounded-3xl (استثناء للـ modal فقط)
  * دخول من اليمين: fadeIn + slideInRight animation
  * الحقول: rounded-full bg-white shadow-md
  * الأزرار: rounded-full مع shimmer effect
  
- المستطيل الأيسر (Hero Background):
  * خلفية: صورة من hero section مع overlay داكن
  * دخول من اليسار: fadeIn + slideInLeft animation
  * عناصر سفلية blur: 
    - backdrop-blur-lg bg-white/20
    - rounded-2xl padding
    - أزرار: "تعرف أكثر" + "تواصل معنا"
```

**التبديل بين Login/Register:**
- زر تبديل فوق حقل الإيميل: "تسجيل" ↔ "تسجيل الدخول"
- تحديث الحالة دون إعادة تحميل المكون
- استخدام `useState` للتبديل بين الوضعين

### 1.2 تعديل Navbar
**الملف:** `components/Navbar.tsx`

**التعديلات:**
```typescript
// إضافة حالة للـ modal
const [isAuthModalOpen, setIsAuthModalOpen] = useState(false);

// عند النقر على أيقونة الحساب:
onClick={() => setIsAuthModalOpen(true)}

// تمرير الحالة للـ AuthModal
<AuthModal 
  isOpen={isAuthModalOpen} 
  onClose={() => setIsAuthModalOpen(false)} 
/>
```

### 1.3 حقول الإدخال
**Login Form:**
- الإيميل (rounded-full input)
- كلمة المرور (rounded-full input with password toggle)
- زر "تسجيل الدخول" (shimmer button)
- رابط "نسيت كلمة المرور؟"

**Register Form:**
- الاسم الأول (rounded-full)
- الاسم الثاني (rounded-full)
- الإيميل (rounded-full)
- كلمة المرور (rounded-full + قوة كلمة المرور)
- تأكيد كلمة المرور (rounded-full + validation)
- زر "إنشاء حساب" (shimmer button)

---

## المرحلة 2️⃣: إعداد البنية التحتية للمصادقة

### 2.1 اختيار نظام المصادقة
**الخيارات المقترحة:**
- ✅ **NextAuth.js (Auth.js v5)** - الأفضل للتكامل مع Next.js 16
- **Clerk** - سهل ولكن مدفوع
- **Supabase Auth** - مجاني ومفتوح المصدر

**القرار:** NextAuth.js v5

### 2.2 تثبيت التبعيات
```bash
npm install next-auth@beta
npm install bcryptjs
npm install @prisma/client prisma
npm install zod
```

### 2.3 إعداد قاعدة البيانات
**الملفات المطلوبة:**
- `prisma/schema.prisma` - مخطط قاعدة البيانات

**المخطط:**
```prisma
model User {
  id            String    @id @default(cuid())
  firstName     String
  lastName      String
  email         String    @unique
  password      String
  role          Role      @default(USER)
  image         String?   @default("/images/default-avatar.png")
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}

enum Role {
  USER
  ADMIN
}
```

### 2.4 إعداد NextAuth
**الملفات المطلوبة:**
- `app/api/auth/[...nextauth]/route.ts` - API routes
- `lib/auth.ts` - تكوين NextAuth
- `lib/authOptions.ts` - خيارات المصادقة
- `middleware.ts` - حماية الصفحات

---

## المرحلة 3️⃣: تطوير API Routes

### 3.1 مسارات التسجيل
**الملف:** `app/api/auth/register/route.ts`

**الوظائف:**
- التحقق من صحة البيانات (Zod validation)
- التحقق من عدم وجود البريد الإلكتروني مسبقًا
- تشفير كلمة المرور (bcrypt)
- إنشاء المستخدم في قاعدة البيانات
- إرجاع استجابة نجاح/فشل

### 3.2 مسارات تسجيل الدخول
**الملف:** `app/api/auth/[...nextauth]/route.ts`

**الوظائف:**
- مزود Credentials للمصادقة بالإيميل وكلمة المرور
- التحقق من بيانات الاعتماد
- إنشاء JWT token
- تخزين الجلسة

### 3.3 مسارات إدارة الملف الشخصي
**الملف:** `app/api/user/profile/route.ts`

**الوظائف:**
- GET: جلب بيانات المستخدم
- PATCH: تحديث البيانات (الاسم، الصورة)
- POST: رفع الصورة الشخصية

---

## المرحلة 4️⃣: واجهة المستخدم العادي

### 4.1 قائمة المستخدم المنسدلة
**الملف:** `components/UserDropdown.tsx`

**العناصر:**
```typescript
// عند النقر على أيقونة الحساب (للمستخدم المسجل):
- الصورة الشخصية + الاسم
- الإعدادات (Settings)
- تسجيل الخروج (Logout)
```

**التصميم:**
- dropdown منسدل من الأعلى
- bg-accent مع shadow
- rounded-2xl
- عناصر مع hover effect (bg-primary/10)

### 4.2 صفحة الإعدادات
**الملف:** `app/profile/settings/page.tsx`

**الأقسام:**
1. **تحديث الصورة الشخصية:**
   - دائرة بها الصورة الحالية أو أيقونة User
   - زر "تغيير الصورة" (file upload)
   - معاينة مباشرة

2. **تحديث المعلومات:**
   - الاسم الأول والثاني
   - الإيميل (readonly)
   - زر "حفظ التغييرات"

3. **تغيير كلمة المرور:**
   - كلمة المرور القديمة
   - كلمة المرور الجديدة
   - تأكيد كلمة المرور
   - زر "تحديث كلمة المرور"

---

## المرحلة 5️⃣: لوحة التحكم الإدارية (Admin Dashboard)

### 5.1 تخطيط Dashboard
**الملف:** `app/admin/layout.tsx`

**المواصفات:**
```typescript
// القائمة الجانبية (Sidebar) على اليمين:
- عرض: 280px
- لون: bg-secondary (الأخضر الداكن #10302b)
- نصوص: text-white
- العناصر:
  * لوحة التحكم (Dashboard)
  * الطلبات (Requests)
  * العقارات (Properties)
  * المستخدمين (Users)
  * الإعدادات (Settings)
  * تسجيل الخروج
```

### 5.2 صفحة لوحة التحكم الرئيسية
**الملف:** `app/admin/page.tsx`

**البطاقات الإحصائية:**
- إجمالي المستخدمين
- الطلبات الجديدة
- العقارات المنشورة
- المراجعات

**التصميم:**
- Grid من البطاقات (4 columns)
- كل بطاقة: bg-white shadow-lg
- أيقونات من lucide-react
- أرقام كبيرة مع Animation (useCounter hook)

### 5.3 صفحة إدارة الطلبات
**الملف:** `app/admin/requests/page.tsx`

**الوظائف:**
- جدول الطلبات مع pagination
- تصفية حسب الحالة (جديد، قيد المعالجة، مكتمل)
- عرض تفاصيل الطلب
- تحديث حالة الطلب

### 5.4 صفحة إدارة المستخدمين
**الملف:** `app/admin/users/page.tsx`

**الوظائف:**
- قائمة المستخدمين
- البحث والتصفية
- تغيير الدور (User ↔ Admin)
- حظر/إلغاء حظر المستخدم

---

## المرحلة 6️⃣: الحماية والأمان

### 6.1 Middleware للحماية
**الملف:** `middleware.ts`

```typescript
// حماية الصفحات:
- /admin/* - للإداريين فقط
- /profile/* - للمستخدمين المسجلين
- إعادة التوجيه للصفحة الرئيسية إذا لم يكن مسموحًا
```

### 6.2 التحقق من الدور
**الملف:** `lib/auth-utils.ts`

**الوظائف:**
- `isAdmin()` - التحقق من دور المستخدم
- `requireAuth()` - طلب تسجيل الدخول
- `requireAdmin()` - طلب صلاحيات الإدارة

### 6.3 أمان API
- التحقق من CSRF token
- Rate limiting للطلبات
- تشفير كلمات المرور (bcrypt - 10 rounds)
- Validation قوية (Zod schemas)

---

## المرحلة 7️⃣: تحسينات UX

### 7.1 رسائل التنبيه
**الملف:** `components/ui/Toast.tsx`

**الحالات:**
- نجاح التسجيل
- نجاح تسجيل الدخول
- فشل المصادقة
- تحديث الملف الشخصي

**التصميم:**
- موقع: أعلى اليمين
- ألوان: success (green), error (red), info (blue)
- animation: slideInDown + fadeOut

### 7.2 Loading States
**المواقع:**
- زر تسجيل الدخول (spinner داخل الزر)
- تحميل الصفحة الشخصية
- رفع الصورة

### 7.3 Error Handling
- رسائل خطأ واضحة بالعربية
- Validation في الوقت الفعلي
- استرجاع البيانات عند الفشل

---

## المرحلة 8️⃣: الاختبار والنشر

### 8.1 الاختبار
**السيناريوهات:**
1. تسجيل مستخدم جديد
2. تسجيل دخول بحساب موجود
3. تسجيل دخول بحساب خاطئ
4. تحديث الصورة الشخصية
5. تغيير كلمة المرور
6. الوصول للـ admin panel
7. تسجيل الخروج

### 8.2 قاعدة البيانات
**الخيارات:**
- ✅ **PlanetScale** - MySQL مجاني
- **Neon** - PostgreSQL serverless
- **Supabase** - PostgreSQL مع واجهة

### 8.3 تكوين البيئة
**الملف:** `.env.local`
```env
DATABASE_URL="mysql://..."
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generated-secret-key"
```

### 8.4 النشر على Netlify
**التحديثات المطلوبة:**
- إضافة environment variables
- تكوين قاعدة البيانات
- اختبار الـ API routes

---

## البنية النهائية للملفات

```
app/
  api/
    auth/
      [...nextauth]/
        route.ts          # NextAuth API routes
      register/
        route.ts          # تسجيل مستخدم جديد
    user/
      profile/
        route.ts          # إدارة الملف الشخصي
      upload/
        route.ts          # رفع الصور
  admin/
    layout.tsx            # تخطيط Admin مع Sidebar
    page.tsx              # لوحة التحكم الرئيسية
    requests/
      page.tsx            # إدارة الطلبات
    users/
      page.tsx            # إدارة المستخدمين
    settings/
      page.tsx            # إعدادات الموقع
  profile/
    settings/
      page.tsx            # إعدادات المستخدم

components/
  auth/
    AuthModal.tsx         # Modal الرئيسي
    LoginForm.tsx         # نموذج تسجيل الدخول
    RegisterForm.tsx      # نموذج التسجيل
    AuthBackground.tsx    # الخلفية اليسرى
  admin/
    Sidebar.tsx           # القائمة الجانبية
    StatsCard.tsx         # بطاقة إحصائية
    RequestsTable.tsx     # جدول الطلبات
    UsersTable.tsx        # جدول المستخدمين
  user/
    UserDropdown.tsx      # قائمة المستخدم المنسدلة
    AvatarUpload.tsx      # رفع الصورة الشخصية
    ProfileForm.tsx       # نموذج تحديث الملف
  ui/
    Toast.tsx             # رسائل التنبيه

lib/
  auth.ts                 # تكوين NextAuth
  authOptions.ts          # خيارات المصادقة
  auth-utils.ts           # دوال مساعدة
  validations/
    auth.ts               # Zod schemas للمصادقة
    user.ts               # Zod schemas للمستخدم

prisma/
  schema.prisma           # مخطط قاعدة البيانات

middleware.ts             # حماية الصفحات

types/
  auth.ts                 # أنواع المصادقة
  user.ts                 # أنواع المستخدم
```

---

## الجدول الزمني المقترح

| المرحلة | المدة | الأولوية |
|---------|-------|----------|
| 1 - واجهة Auth Modal | 2-3 أيام | 🔴 عالية |
| 2 - البنية التحتية | 3-4 أيام | 🔴 عالية |
| 3 - API Routes | 2-3 أيام | 🔴 عالية |
| 4 - واجهة المستخدم | 2-3 أيام | 🟡 متوسطة |
| 5 - لوحة الإدارة | 4-5 أيام | 🟡 متوسطة |
| 6 - الحماية | 2 أيام | 🔴 عالية |
| 7 - تحسينات UX | 2-3 أيام | 🟢 منخفضة |
| 8 - الاختبار والنشر | 2-3 أيام | 🔴 عالية |

**المدة الإجمالية:** 3-4 أسابيع

---

## ملاحظات مهمة

### الالتزام بالتصميم الحالي:
✅ استخدام الألوان من `tailwind.config.js`:
- Primary: `#edbf8c` (ذهبي فاتح)
- Secondary: `#10302b` (أخضر داكن)
- Accent: `#ead3b9` (بيج فاتح)
- White: `#fdf6ee` (بيج أبيض)

✅ الأنماط:
- Sharp corners للعناصر الأساسية (border-radius: 0)
- rounded-full للحقول والأزرار الدائرية
- rounded-2xl/3xl للـ modals والبطاقات فقط
- shimmer effect على الأزرار
- backdrop-blur على العناصر الزجاجية

✅ الخطوط:
- Dubai للعربية
- Bristone للإنجليزية

✅ الأنيميشن:
- استخدام Framer Motion
- fadeIn, slideIn variants من `lib/animations/variants.ts`
- الالتزام بـ timing من `lib/animations/config.ts`

---

## الخطوة التالية

**هل تريد البدء في:**
1. ✅ تنفيذ المرحلة 1 (واجهة Auth Modal)؟
2. ⚙️ إعداد قاعدة البيانات أولاً؟
3. 📋 مراجعة وتعديل الخطة؟

**أخبرني لنبدأ التنفيذ! 🚀**
